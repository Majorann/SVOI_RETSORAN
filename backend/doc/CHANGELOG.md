# История изменений

## 11.02.2026

### Редизайн UI (тёплая тёмная тема)
- Интерфейс переведён на тёплую тёмную палитру без синего неона.
- Обновлены токены цветов, контраст, поверхности и бордеры в `backend/static/css/style.css`.
- На главной странице пересобран порядок секций и улучшена визуальная иерархия в `backend/templates/index.html`.

### Типографика
- Весь интерфейс переведён на шрифт Inter:
  - подключение шрифта в `backend/templates/base.html`;
  - обновление `font-family` в `backend/static/css/style.css`.

### Схема зала и бронирование
- Доработана панель бронирования на схеме:
  - улучшены размеры, позиционирование и стабильность отображения;
  - исправлены отступы и внутренний layout формы.
- Восстановлен и усилен blur схемы:
  - общий blur при активной панели;
  - дополнительное затемнение и усиление blur во время ввода данных (`is-typing`).
- Доработан неон от столиков:
  - центр свечения привязан к выбранному столику;
  - улучшена форма свечения и убраны артефакты прямоугольного контейнера;
  - настроена интенсивность свечения для свободных и занятых столиков.

### Динамика фона по времени
- Реализована смена фона по выбранному времени бронирования (09:00–22:00) в `backend/static/js/app.js`.
- Добавлено мягкое смешивание оттенка времени с базовым фоном без резкой границы в `backend/static/css/style.css`.
- Обновлена палитра утро/день/вечер/ночь под медно-терракотовые тона.

### Анимации и взаимодействие
- Добавлен плавный индикатор активного раздела в нижней навигации (перетекающая капсула).
- Добавлена плавная анимация выбора времени в правом таймлайне (`timeline__active-indicator`).

### Нижняя навигация
- Нижняя навигация переведена в режим «только иконки» (без текста и эмодзи).
- Подключены PNG-иконки:
  - `backend/static/img/bell.png`
  - `backend/static/img/home.png`
  - `backend/static/img/profile.png`
- Настроены размеры иконок и позиционирование бейджа уведомлений.

### Статусы и цветовая система
- Унифицированы зелёные/красные статусы интерфейса:
  - зелёный: `#5E7F63`;
  - красный: `#7A3E3E`.
- Обновлены состояния кнопок, таймлайна, статусов столов и ошибок в едином стиле.

## 12.02.2026

### Исправления багов и ошибок

- Исправлен заметный микросдвиг нижней навигации между страницами:
  - добавлены `scrollbar-gutter: stable` и fallback `overflow-y: scroll` в `style.css`.
- Исправлено позиционирование неона на схеме зала:
  - центр неона теперь вычисляется от `.hall__map`, а не от контейнера свечения.
- Исправлен конфликт и дублирование неон-слоев на схеме:
  - удален первый (устаревший) блок фонового неона, оставлен один актуальный.
- Исправлен профиль красного неона занятых столов:
  - красный свет использует ту же геометрию свечения, что и зеленый, меняется только цвет.
- Исправлен визуальный разрез между фото блюда и фоном карточки при hover:
  - скорректированы размеры и смещение переходного градиента (`мыло`) и параметры масштаба.
- Исправлена видимость красного фонового tint:
  - эффект оставлен только на странице выбора столика (`/reserve`), убран с остальных страниц.
- Исправлены проблемы с текстовой кодировкой в `app.py`.

### Улучшения интерфейса и UX

- Главная:
  - переработан блок «Меню → Популярное»:
    - `type` вынесен в строку заголовка рядом с названием блюда;
    - обновлен стиль `type` (белый текст, рамка `rgb(216,163,127)`).
- Профиль:
  - добавлена кнопка удаления карты справа в каждой карточке оплаты;
  - стилизована как закругленный квадрат;
  - добавлена иконка мусорки из `static/img/trash.png`;
  - добавлен серверный маршрут `POST /cards/delete` с удалением карты и корректным пересчетом `active`.
- Меню:
  - полностью обновлен блок управления:
    - фильтр категорий в виде горизонтальных chips;
    - отдельный компактный dropdown сортировки;
    - сортировка: `По популярности`, `Цена ↑`, `Цена ↓`;
    - фильтр и сортировка работают совместно без перезагрузки;
    - добавлена анимация перестройки карточек (`fade + slide`, ~180ms).
  - добавлена поддержка популярности в модели блюда:
    - поля `popularity/orders_count` читаются из `menu_items/*/item.txt`.
  - hover-стиль карточек меню доработан:
    - `lore` становится белым;
    - фон карточки в hover: `#3C342D`;
    - увеличен масштаб карточки на hover;
    - неон у карточки переведен в тон акцента (`#cc7b57`) и вынесен под карточку (светит в фон).
  - активные filter-chip получили неоновую подсветку акцентного цвета.
- Промо и акции:
  - добавлен формат `promo_items` с поддержкой:
    - `reklama`: `text`, `link`;
    - `akciya`: `name`, `lore`;
    - опционального `photo.png`.
  - добавлена загрузка promo-блоков в `app.py` и преобразование в карточки блока «Что нового».
- Декоративные изображения (desktop):
  - на главной добавлен правый персонаж `human1_general.png`;
  - в уведомлениях добавлены:
    - справа `human2_frame_ball.png`,
    - слева `Vorona.png`.
  - позиционирование: по высоте по центру, справа/слева от интерфейса, только на широких экранах.
- Шаблоны страниц:
  - в `base.html` добавлен блок `body_class` для page-scoped стилей;
  - используются классы `page-index`, `page-reserve`, `page-notifications`.

### Изменения данных и конфигов

- Очищен `NEWS_CARDS` в `app.py` (убраны заглушки акций по умолчанию).
- Добавлены и обновлены ресурсы:
  - `backend/static/img/trash.png`
  - `backend/static/img/human1_general.png`
  - `backend/static/img/human2_frame_ball.png`
  - `backend/static/img/Vorona.png`
  - `backend/static/promo_items/reklama/item.txt`
  - `backend/static/promo_items/akciya/item.txt`

### Спринт 2 (12.02.2026)

- ### Оформление заказа и оплата:
  - добавлен полноценный flow `checkout -> payment -> confirm` с хранением заказов в `orders.json`;
  - добавлены страницы `checkout`, `payment`, `orders`, `order-detail`;
  - реализовано списание баллов при оплате (`use_points`, `points_applied`, `payable_total`);
  - добавлена валидация состояний оплаты: нет брони / бронь устарела / пустой заказ / нет карты;
  - переработана страница оплаты в state machine: `default`, `processing`, `success`, `error` (строго одно состояние на экране).

- ### Меню и корзина:
  - убрано управление количеством из карточек блюд;
  - кнопка в карточке переведена в режим `В корзину` / `Убрать` (`#7A3E3E`) в зависимости от наличия позиции в корзине;
  - корзина переработана в автопанель справа: появляется при первом товаре, скрывается при пустой корзине;
  - добавлены анимации корзины: slide/fade, settle, stagger, pop-in, remove, pulse суммы;
  - добавлена анимация изменения количества (роллер-эффект) в корзине и на странице checkout;
  - исправлены баги корзины: ложный блок “Корзина пуста”, отсутствие фото у позиций, мерцание списка при `+/-`.

- ### Фильтрация меню:
  - исправлена фильтрация категорий (нормализация типов + принудительное скрытие карточек), чтобы блюда не показывались в чужих категориях.

- ### Уведомления и статусы заказов:
  - добавлен пуш-тип уведомления `Заказ готовится`;
  - на главной странице добавлен блок статусов заказов (без состава и суммы);
  - в `Уведомлениях` добавлен состав заказа и сумма;
  - бейдж уведомлений учитывает брони + активные уведомления по заказам;
  - добавлено “окно готовки” 20 минут: уведомление `Заказ готовится` показывается только в расчётный интервал и автоматически скрывается после него.

- ### Исправления шаблонов:
  - устранены `TypeError` в шаблонах (`builtin_function_or_method is not iterable`) за счёт перехода на безопасный доступ через `.get(...)` в Jinja.

## 01.03.2026

### Мобильное бронирование (reserve)

- Переработан mobile-flow выбора столика:
  - добавлена компактная панель текущих `дата/время` с кнопкой `Изменить`;
  - панель бронирования переведена в формат выезжающего блока снизу (bottom sheet);
  - добавлен отдельный мобильный блок выбора даты/времени (`datetime-sheet`);
  - добавлен `sheet-backdrop` и синхронизация состояний между блоками.
- Улучшена мобильная эргономика схемы:
  - увеличены tap-зоны столов;
  - скорректированы размеры текста/контролов;
  - правая desktop-таймлиния скрывается на мобильных устройствах.

### Дата и время: валидация и состояния

- Добавлена маска мобильной даты в формате `ДД.ММ.ГГГГ` с автоподстановкой разделителей.
- При фокусе мобильного поля даты открывается нативный date picker через скрытое `type="date"` поле.
- Добавлена проверка «не раньше текущего момента»:
  - невалидные дата/время подсвечиваются унифицированным красным (`--status-red`, `#7A3E3E`);
  - кнопки `Применить` и `Забронировать` блокируются до исправления.
- Для desktop и mobile добавлен сценарий:
  - если выбраны прошлые дата/время, все столы на схеме принудительно отображаются как занятые (красные).

### Кнопки mobile-sheet

- Кнопка `Изменить` оформлена как pill:
  - `border-radius: 9999px`;
  - акцентный цвет и мягкое неоновое свечение.
- Кнопка `Закрыть` заменена на иконку `×` без фона и бордера.
- Кнопка `Применить` обновлена:
  - полноширинная;
  - зелёная (`#5E7F63`) с лёгкой тенью;
  - `active`-состояние через `scale(0.98)`.

### Медиа-ресурсы

- Добавлена поддержка обоих форматов фото в контенте:
  - `photo.png` и `photo.webp` для `menu_items` и `promo_items`.
- Обновлены изображения в интерфейсе:
  - на главной: `human1_general.webp`;
  - в уведомлениях: `human2_frame_ball.webp`, `Vorona.webp`.

### Цвета и типографика

- Глобально обновлён оранжевый акцент до `#DA7756` (с сохранением фоновых оттенков блока столиков по запросу).
- Интерфейс переведён на `Georgia`:
  - удалено подключение `Inter` из `base.html`;
  - обновлён `font-family` в `style.css`.

## 05.03.2026

### Иконки (SVG)

- Заменены PNG-иконки интерфейса на SVG-версии из `assets`:
  - нижняя навигация: `bell`, `home`, `profile`;
  - иконка баллов на главной: `rubl`;
  - иконка удаления карты в профиле: `trash`.
- Обновлены пути в шаблонах и стилях:
  - `backend/templates/index.html`
  - `backend/templates/profile.html`
  - `backend/static/css/style.css`
- Добавлены SVG-файлы в `backend/static/img/`.

### Нижняя навигация: неон-переключение

- Добавлен неон-пульс для иконки вкладки при переключении:
  - эффект запускается уже на `pointerdown`, чтобы отображаться с первого нажатия;
  - эффект дублируется на `click` для стабильности;
  - задержка перехода между страницами увеличена до `300ms`, чтобы пульс успевал проявиться.
- Усилен и продлён визуальный эффект:
  - анимация `navNeonPulse` увеличена до `1600ms`;
  - добавлены дополнительные слои `drop-shadow`;
  - реализовано плавное затухание свечения к концу анимации.

### Технические комментарии

- В `backend/static/js/app.js` и `backend/static/css/style.css` добавлены поясняющие комментарии к новым блокам:
  - логика старта неон-пульса;
  - обоснование задержки навигации;
  - назначение и профиль анимации свечения.

## 05.03.2026

### Техническая доработка (образовательный проект)

- Добавлена повторная проверка на стороне сервера в запросе `POST /payment/confirm`:
  - проверка активного бронирования в момент подтверждения;
  - проверка активной платежной карты;
  - повторная проверка и нормализация позиций заказа;
  - повторный расчет `items_total`, `points_applied`, `payable_total` на стороне сервера.

- Добавлены простые файловые блокировки для хранилищ JSON (`*.lock`) и критические операции чтения, изменения и записи:
  - регистрация пользователя;
  - создание заказа и списание баллов;
  - создание и отмена бронирования;
  - добавление/удаление карты. 

 - Добавлена базовая защита от CSRF-атак:
  - генерация токена CSRF в сеансе;
  - проверка токена для `POST/PUT/PATCH/DELETE`;
  - `<мета-имя="csrf-токен">`в `base.html`;
  - скрытый токен CSRF во всех формах "method="post"`;
  - "fetch("/book")" теперь отправляет заголовок "X-CSRF-токен".

- Также исправлена начальная активная вкладка нижней навигации для неавторизованного профиля-> вход в систему / регистрация.

## 05.03.2025

### Статусы заказов, уведомления и бонусы

- Добавлен единый статус-бар заказа на главной:
  - таймлайн `Готовится (15 мин) -> Заказ несут (1 мин) -> Заказ выдан (1 мин) -> скрытие`;
  - корректное восстановление состояния при повторном входе в приложение;
  - поддержка нескольких активных заказов с приоритетом ближайшего к выдаче.
- Добавлен рендер статусов в уведомлениях:
  - заголовок статуса, остаток времени `mm:ss`;
  - отображение суммы заказа и начисленных бонусов.
- Добавлено начисление бонусов `5%` от суммы к оплате:
  - сохранение `bonus_earned` в заказ;
  - обновление баланса пользователя с учётом списанных баллов и начисления бонуса.

### Checkout/Payment UX fixes

- Исправлен блок `К оплате` на checkout:
  - сумма и валюта вынесены на отдельную строку;
  - устранён перенос символа `₽` при 4+ значных суммах.
- Исправлено залипание кнопок после возврата назад (BFCache):
  - на checkout кнопка `Перейти к оплате` корректно восстанавливается;
  - на payment кнопка оплаты корректно выходит из состояния `Обработка...`.

### Карточка «Мои баллы» (главная)

- Удалён подзаголовок `Бонусы и уровни`.
- Удалён отдельный раздел `/points` (роут перенаправляет на главную).
- Переработана компоновка карточки:
  - крупный баланс, табличные цифры (`tabular-nums`);
  - добавлены иконки `coins.svg` и `rubl.svg` в нижний блок;
  - обновлены размеры и позиционирование элементов, устранены наезды текста.

### Иконки и визуальные правки

- Добавлены SVG-иконки в `backend/static/img/`:
  - `frying-pan-svgrepo-com.svg`, `waiter-svgrepo.svg`, `coins.svg` и др.
- В статус-баре:
  - для `Готовится` используется иконка сковороды;
  - для `Заказ несут` используется `waiter-svgrepo.svg`;
  - увеличены размеры контейнера иконки.
- В блоке «Меню -> Популярное»:
  - цена перемещена в нижнюю левую часть контентного блока;
  - увеличен размер ценника и добавлен дополнительный отступ от описания.

## 10.03.2026

### Модульная реорганизация backend

- Проект переведён с монолитной структуры на модульную:
  - добавлены `backend/routes/`, `backend/services/`, `backend/storage/`, `backend/models/`.
- Роуты вынесены в отдельные файлы:
  - `auth_routes.py`, `booking_routes.py`, `profile_routes.py`, `orders_routes.py`, `menu_routes.py`, `main_routes.py`.
- Бизнес-логика вынесена в `backend/services/business_logic.py`:
  - функции расчётов статусов заказа, бронирования, нормализации данных заказа и времени подачи.
- Работа с JSON-хранилищами вынесена в `backend/storage/json_store.py`:
  - единый слой чтения/записи `bookings`, `orders`, `users`.
- Добавлен `backend/config.py`:
  - централизованы пути, константы, схема столов, стены, шаги статусов заказа.

### Модели данных

- Добавлены dataclass-модели:
  - `MenuItem`, `PromoItem`, `Booking`, `Order`, `User` в `backend/models/`.
- Парсинг меню и промо переведён на модели с сохранением прежнего формата выходных словарей.

### Frontend: модульный JavaScript

- `backend/static/js/app.js` преобразован в entrypoint ES modules.
- Код разнесён по модулям:
  - `core.js`, `menuHoverMood.js`, `bottomNavMotion.js`, `tableTooltip.js`, `orderStatusBar.js`, `pointsBalanceCard.js`.
- В `backend/templates/base.html` подключение JS переведено на:
  - `<script type="module" ...>`.

### Меню и корзина (desktop/zoom)

- Улучшена адаптация правой корзины при большом масштабе интерфейса:
  - убраны жёсткие привязки, добавлены адаптивные `max-height`/`width` с учётом viewport/safe-area;
  - исправлена внутренняя прокрутка списка корзины (`min-height: 0` для grid-контейнера).
- Добавлена адаптация каталога:
  - при открытой корзине и недостаточной ширине рабочей области карточки меню переключаются в 1 колонку.
- Улучшена анимация закрытия корзины:
  - синхронизированы JS-таймер и CSS-transition;
  - устранён рывок контента при закрытии (`menu-cart-open` снимается после завершения анимации).

### Профиль и привязка карты

- Усилена валидация имени держателя карты:
  - автотранслитерация русских букв в латиницу по таблице соответствий;
  - нормализация к `A-Z`, пробелу и `-`, `UPPERCASE`.
- Исправлен ввод имени держателя:
  - пробел между словами больше не «съедается» во время набора.
- Добавлена строгая проверка срока карты на клиенте и сервере:
  - формат только `MM/YY`;
  - месяц только `01..12`;
  - срок не может быть в прошлом.
- Обновлён placeholder поля имени карты в профиле:
  - `NAME SURNAME`.

### UI/UX доработки (mobile + desktop)

- Меню (`/menu`): переработан верхний header:
  - кнопка назад переведена на SVG-иконку и повёрнута в стандартное направление «влево»;
  - заголовок `МЕНЮ` усилен (uppercase, увеличенный размер, жирный вес, градиентный текст);
  - убран подзаголовок `Выберите блюда`;
  - выравнивание заголовка сделано визуально центрированным для mobile и desktop.

- Мобильная корзина в меню переведена в формат FAB + bottom sheet:
  - добавлена фиксированная круглая кнопка корзины (`#DA7756`) с бейджем количества;
  - корзина открывается поверх контента через overlay, без сдвига карточек меню;
  - добавлены пустое состояние корзины и кнопка сворачивания в шапке панели;
  - добавлено закрытие свайпом вниз (порог закрытия `30%` высоты панели), при меньшем смещении панель возвращается обратно;
  - учтены safe-area отступы и совместимость с нижней навигацией.

- Исправлено поведение карточек блюд на мобильных:
  - раскрытие описания переведено на явное состояние `.is-expanded` по тапу по карточке;
  - нажатия на кнопку `В корзину` больше не ломают/не переключают раскрытие карточки;
  - добавлено автосворачивание других карточек и закрытие раскрытия по тапу вне карточек.

- Статус-бар заказа (`now bar`) визуально и функционально обновлён:
  - добавлены стадии `Готовим` / `Несём` / `Заказ выдан` с пулом случайных фраз без повтора подряд;
  - комментарий статуса анимируется в стиле typewriter (стирание + пауза + печать) с мигающим caret;
  - заголовок стадии получил эффект «электронного табло» (scramble) при смене стадии;
  - прогресс-бар переработан: более тонкий премиальный трек, тёплый градиент заполнения, мягкое свечение, shimmer и плавный переход ширины;
  - добавлены адаптивные настройки для desktop/mobile и `prefers-reduced-motion` для прогресс-анимаций/caret.

- Карточка `Мои баллы` доработана для длинных значений баланса:
  - устранено обрезание цифр на mobile/desktop для 4+ значных сумм;
  - монета (`coins`) затемнена и выведена из давления на layout;
  - скорректированы размеры/отступы и масштаб числа для более стабильного вмещения.

## 11.03.2026

### Now bar / статусы заказа

- Добавлен live-endpoint `GET /api/order-statuses` в `backend/app.py` для актуализации статусов без перезагрузки страницы.
- В `backend/static/js/modules/orderStatusBar.js` добавлен polling статусов (каждые 5 сек) и синхронная перерисовка бара:
  - бар корректно исчезает после завершения всех активных этапов;
  - статусы больше не «зависают» до ручного обновления страницы.
- Исправлен таймер now bar:
  - визуальный отсчёт обновляется каждую секунду между poll-запросами;
  - серверный опрос остаётся раз в 5 секунд только для синхронизации стадии.

### Delivery progress / грузовик

- Для delivery-стадий переработан показ дорожного прогресса:
  - `delivery_cooking` — дорожный вариант без машины;
  - `courier_sent` и `delivery_delivering` — дорожный вариант с машиной.
- Заменены иконки доставки в статусах на `truck.svg` в `orderStatusBar.js` (убран случайный показ `waiter-svgrepo.svg`).
- В дорожном прогрессе заменён спрайт машины на `truck2.svg`:
  - файл добавлен в `backend/static/img/truck2.svg`;
  - увеличен размер машины и скорректировано позиционирование по правому краю заполненной части прогресса.
- Добавлена и усилена анимация «езды» грузовика (`deliveryTruckBounce`) в `style.css`.
- Для `prefers-reduced-motion` сохранён мягкий режим анимаций, но движение грузовика оставлено в щадящем варианте.

### Главная страница

- Карточка `Доставка` (`backend/templates/index.html`, `backend/static/css/style.css`):
  - удалена плашка с эмодзи;
  - `truck.svg` вынесен в фоновый декоративный слой карточки;
  - фон грузовика масштабирован почти на всю плашку и адаптирован для mobile.
- Кнопка `Заказать столик` на главной (`.hero__cta.primary-action`) получила акцентный неоновый glow:
  - отдельные состояния `default/hover/active`;
  - без влияния на остальные кнопки `.primary-action`.

## 12.03.2026

### Меню и контент

- Исправлена загрузка меню из файлов:
  - Теперь программа ожидает ***.webp, а не photo.webp;
  - добавлены/обновлены каталоги блюд в `backend/static/menu_items/`.
- На главной в блоке `Меню → Популярное` увеличен лимит карточек до `10`.
- Для блока `Популярное` добавлена ротация набора блюд после перезапуска приложения (при количестве блюд больше лимита).
- Для карточек меню улучшено поведение при длинных названиях:
  - стабилизировано выравнивание строки названия/типа;
  - устранены съезды цены и кнопки `В корзину`.
- Унифицирован цвет `type`-бейджей категорий блюд:
  - салаты и алкогольные напитки приведены к стилю горячих блюд.

### Бронирования, заказы и now bar

- Усилена надёжность отмены заказа при отмене столика:
  - сценарий отмены брони и связанных заказов сделан консистентным.
- Пересмотрена логика ожидания и старта готовки заказа.
- В now bar добавлен приоритет показа:
  - отображается заказ с ближайшей подачей/самой быстрой доставкой.
- Исправлены скачки таймера now bar (редкие перескоки на 2 секунды).

### Даты и уведомления

- Стандартизирован формат времени в уведомлениях: `ДД.ММ.ГГ`.
- Тот же формат применён в блоке `Ваши брони` на главной (`date_display` в шаблоне).

### Авторизация и регистрация (телефон)

- Добавлена валидация номера телефона на login/register.
- В маску телефона добавлен префикс `+7` по умолчанию.
- Исправлено поведение редактирования номера:
  - `+7` не удаляется;
  - устранено «залипание» первых цифр;
  - убрано повторное добавление лишних `7`.
- Для корректно заполненного номера добавлена мягкая унифицированная зелёная подсветка рамки поля.
- Кнопки `Войти` и `Зарегистрироваться` приведены к стилю главной кнопки `Забронировать столик`.

### UI-фиксы главной страницы

- Убрана принудительная вертикальная прокрутка (`scroll`) для устранения визуальных артефактов.
- Доработана карточка `Мои баллы`:
  - исправлено подрезание числа баланса снизу;
  - стабилизировано отображение символа `₽`.

### Карточка доставки: анимация грузовика

- Добавлена hover-анимация карточки доставки только на CSS (`keyframes`):
  - тряска грузовика (`translate/rotate`);
  - линии «ветра» с движением назад и затуханием.
- Анимация реализована внутри контейнера карточки с контролируемыми слоями и `overflow`.
- За основу взят существующий `truck.svg` (без кастомных колёс).
- Исправлены конфликты слоёв/перекрытий (`z-index`, псевдослой, область SVG).
- Исправлены предупреждения CSS-парсера в `templates/index.html` (кавычки в inline `background-image`).

### Дополнительные UI/UX правки (после 12.03.2026)

- Нижняя навигация переведена на новые SVG-иконки:
  - уведомления: `bell_pixel.svg`;
  - главная: `home2.svg`;
  - профиль: `profile2.svg`.
- Для иконки `Главная` в нижнем nav увеличен размер на mobile (x2 относительно прежнего).

- В шапках страниц заменён квадрат с буквой `С` на SVG-логотип:
  - используется `orelll.svg` из `assets`, подключён через `static/img/orelll.svg`;
  - обновлены все шаблоны с блоком `.brand__mark`, включая `menu`.

- Исправлен mobile-баг в `now/how bar`:
  - длинные статусные фразы больше не наезжают на иконку;
  - таймер выровнен вправо, текст статуса отображается рядом корректно.

- Исправлен mobile-баг выбора столов в бронировании:
  - при отмене выбора и выборе другого стола предыдущий активный `table--hovered` теперь корректно сбрасывается;
  - устранено «накопление» визуальной ошибки.

- Для mobile-версии уменьшен заголовок `МЕНЮ / МЕНЮ ДОСТАВКИ` до `0.75x`.
- Для `МЕНЮ ДОСТАВКИ` на mobile выполнены точечные правки позиционирования и выравнивания (правый край + смещение).

- На странице блюда (`/menu/<id>`) переработан блок фото:
  - фото сделано более широким/крупным на desktop (почти full-bleed);
  - на mobile добавлены жёсткие ограничения размеров, чтобы изображение не выходило за экран.

- В delivery progress (now/how bar) заменён внутренний грузовик:
  - убрана старая модель внутри бара;
  - подключён `truck2.svg` как источник машинки в progress bar.
- Добавлен эффект «ветра» в progress bar доставки, как в карточке `Доставка` на главной:
  - три линии со stagger-задержками;
  - анимация сдвига назад и затухания;
  - линии скрываются, когда машина в bar отключена (`data-delivery-vehicle="off"`).

- Исправлено отображение даты/времени в уведомлениях для доставки:
  - для `order_type=delivery` больше не используется пустой `booking.time`;
  - дата/время берутся из `created_at`, вместо заглушек вида `--:--`;
  - в карточке уведомления для delivery выводится метка `Доставка` вместо `Стол №—`.

## 13.03.2026

### Postgres / Neon: подключение и хранение

- Добавлен backend-хранилище Postgres `backend/storage/pg_store.py`:
  - чтение/запись `users`, `bookings`, `orders` через `app_state` (JSONB);
  - автоинициализация таблицы `app_state` и seed-ключей.
- Добавлен SQL-скрипт инициализации Neon:
  - `backend/sql/neon_init.sql`.
- Добавлен скрипт миграции JSON -> Neon:
  - `backend/ops/migrate_json_to_neon.py`.
- В `backend/app.py` добавено автоматическое переключение хранилища:
  - при наличии `DATABASE_URL` используется Postgres;
  - при ошибке подключения выполняется fallback на JSON.
- В `requirements.txt` добавлен драйвер `psycopg[binary]`.

### Сессии и стабильность профиля

- Улучшена cookie-конфигурация для hosted/iframe-сценариев:
  - `SESSION_COOKIE_SAMESITE`, `SESSION_COOKIE_SECURE`, `SESSION_COOKIE_PARTITIONED`, `TRUST_PROXY_HEADERS`.
- В `keep_user_session` убраны лишние проверки на статике:
  - статические запросы больше не инициируют чтение пользователя из хранилища.
- Снижен риск ложного logout:
  - профиль больше не выкидывает пользователя сразу при единичном transient-сбое чтения записи пользователя.

### Производительность

- Для Postgres-хранилища добавлено переиспользование соединения в потоке:
  - снижены накладные расходы на создание нового коннекта при каждом чтении/записи.
- Добавлен debug endpoint `GET /debug/storage`:
  - отображает активный backend (`json/postgres`), пути и счётчики сущностей.

### Очистка заказов

- Добавлена авто-очистка массива заказов:
  - активные заказы сохраняются;
  - завершённые/устаревшие удаляются по TTL.
- Добавлены env-настройки:
  - `ORDER_RETENTION_DAYS` (по умолчанию `7`);
  - `ORDER_PRUNE_INTERVAL_SECONDS` (по умолчанию `60`).

### How/Now bar: таймер и время

- Исправлена проблема таймера `00:00:00` сразу после оформления заказа:
  - в `orderStatusBar.js` ISO-время без таймзоны теперь интерпретируется корректно.
- Исправлен timezone-сдвиг в расчётах времени брони/ожидания:
  - в `business_logic.py` добавлено приведение локального времени брони к UTC;
  - добавлена env-настройка `APP_TIMEZONE` (по умолчанию `Europe/Kaliningrad`).

## 14.03.2026

### HF / storage fallback

- Исправлен сценарий `Internal Server Error` на первом заходе в HF Space:
  - при наличии `DATABASE_URL` приложение теперь валидирует доступность Postgres сразу на старте;
  - если импорт или подключение к Postgres не удаётся, backend безопасно остаётся на JSON-хранилище;
  - убрано падение первого `GET /` из-за ленивого обращения к недоступной БД.

### Меню: категории и анимации

- Для категории алкогольных блюд укорочена подпись:
  - `Алкогольные напитки` -> `Алкоголь` в данных меню.
- Добавлена направленная анимация переключения категорий в меню:
  - при переходе вправо текущий список уходит влево, новый приходит справа;
  - при возврате по иерархии анимация зеркалится.
- Для анимации категорий добавлен viewport-контейнер списка:
  - переходы выполняются слоем поверх меню без пересборки страницы.
- Исправлен визуальный баг во время слайда категорий:
  - карточки блюд больше не «растягиваются» в момент анимации.

### Меню на mobile

- Для mobile-версии меню добавлен отдельный выпадающий фильтр `Тип` рядом с сортировкой:
  - использует те же категории, что и desktop-чипы;
  - связан с общей логикой фильтрации и анимации списка.
- На mobile скрыта длинная горизонтальная лента категорий:
  - вместо неё используются два компактных контрола: `Тип` и `Сортировать`.

### Корзина на mobile

- Для открытой mobile-корзины добавлена текстовая подсказка:
  - `Нажмите чтобы свернуть`.
- Подсказка показывается только если над корзиной остаётся достаточно свободного пространства:
  - при сильном перекрытии экрана текст не выводится.
- Подсказка позиционируется по центру свободной зоны между верхом экрана и верхней границей корзины.
- Добавлены плавные анимации появления и скрытия подсказки:
  - мягкое всплытие;
  - плавное исчезновение;
  - показ выполняется после полного раскрытия корзины.
- Убран эксперимент с variable-font анимацией для подсказки:
  - оставлена более сдержанная версия без смены толщины шрифта.

### Главная страница: бренд-блок

- На главной странице удалена подпись `Домашний ресторан`.
- Бренд `Свои` переработан под стиль заголовка `МЕНЮ`:
  - uppercase;
  - светлый градиент текста;
  - усиленный вес и растяжение по оси X.
- Размер `СВОИ` откалиброван отдельно для desktop и mobile.
- Исправлен CSS-каскад для главной:
  - более поздний `.brand__name` больше не сбрасывает размер домашнего бренда.

### Верхняя кнопка Home

- Исправлено центрирование иконки домика в верхней кнопке перехода на главную:
  - устранён сдвиг SVG внутри кнопки в уведомлениях, бронировании и других шапках.
- Подобран увеличенный размер домика без увеличения самой кнопки:
  - иконка остаётся внутри контейнера и не вылезает за его границы.

## 14.03.2026 (2)

### Надёжность Postgres / Neon
- Добавлен фоновый keepalive для Postgres/Neon в `backend/app.py`:
  - запускается только при активном `DATABASE_URL`;
  - периодически пингует БД через `SELECT 1`;
  - не роняет приложение при временной ошибке соединения.
- В `backend/storage/pg_store.py` добавлены `ping()` и сброс stale-соединения с переподключением.
- Убрано автоматическое переключение на JSON при наличии `DATABASE_URL`:
  - если Postgres недоступен, backend теперь падает сразу на старте;
  - исключён сценарий с двумя разными источниками данных.
- Добавлен опциональный Redis-кэш меню:
  - включается через `REDIS_URL`;
  - кэширует собранный список блюд по TTL;
  - при недоступности Redis backend читает меню напрямую из файлов без падения.
- В `backend/README.md` задокументированы env-параметры:
  - `DB_KEEPALIVE_ENABLED`
  - `DB_KEEPALIVE_INTERVAL_SECONDS`
  - `REDIS_URL`
  - `MENU_CACHE_ENABLED`
  - `MENU_CACHE_TTL_SECONDS`
  - `MENU_CACHE_KEY`

## 14.03.2026(3)

### Надёжность Neon / Postgres
- Усилен старт backend при работе с Neon:
  - добавлены повторные попытки подключения на старте (`POSTGRES_STARTUP_RETRIES`, `POSTGRES_STARTUP_RETRY_DELAY_SECONDS`);
  - увеличен `connect_timeout` для Postgres;
  - операции чтения/записи и `ping()` в `backend/storage/pg_store.py` теперь умеют переподключаться и повторяться при кратковременном сетевом сбое (`DB_OPERATION_RETRIES`, `DB_RETRY_DELAY_SECONDS`).
- В режиме Postgres запись больше не зависит от локальных `*.lock`-файлов:
  - добавлен `storage_write_lock`, который использует файловую блокировку только в JSON-режиме;
  - это убирает лишнюю зависимость от файловой системы контейнера при работе через Neon.

### Docker / HF окружение
- Для контейнерного запуска обновлён `Dockerfile`:
  - `gunicorn` запускается с увеличенным `--timeout`, чтобы cold start Neon не убивал worker слишком рано.
- Для локального `docker-compose` дефолты разделены с hosted-сценарием:
  - локально по умолчанию используются `SESSION_COOKIE_SECURE=false`, `SESSION_COOKIE_SAMESITE=Lax`, `SESSION_COOKIE_PARTITIONED=false`, `TRUST_PROXY_HEADERS=false`;
  - для HF переменные должны переопределяться через `Variables and secrets`.
- Уточнена эксплуатация HF:
  - `DATABASE_URL` и `FLASK_SECRET_KEY` должны задаваться именно в `Secrets`, иначе backend остаётся на JSON.

### Отладка и мобильный вход
- Эндпойнт `/debug/storage` закрыт по умолчанию и включается только через `DEBUG_STORAGE_ENABLED=true`.
- Временное логирование мобильных запросов удалено после диагностики.
- На страницах входа и регистрации добавлена подсказка:
  - если встроенный браузер (например, Telegram) не удерживает сессию, следует открыть сайт в Chrome.

### Загрузка меню
- Усилена загрузка блюд из `backend/static/menu_items/*/item.txt`:
  - если у блюда некорректный `id` (пустой, строковый или невалидный), backend теперь автоматически назначает ему следующий свободный числовой `id`;
  - если у нескольких блюд совпадает `id`, первый сохраняет исходное значение, а следующим автоматически выдаются новые свободные `id`;
  - конфликтные и невалидные `id` логируются в startup/runtime log, сами `item.txt` при этом не переписываются.

## 15.03.2026

### Главная страница и новости
- Переработан hero-блок на главной:
  - добавлен декоративный столик в `backend/templates/index.html`;
  - обновлён правый персонаж, вместо `human1_general.webp` теперь используется `person3.svg`.
- Блок новостей переведён на раскрывающиеся карточки:
  - карточки теперь рендерятся как `article` с отдельной кнопкой-ссылкой `Открыть`;
  - добавлены раскрытие по клику и по клавиатуре, а также анимации открытия/закрытия в `backend/static/js/app.js`;
  - обновлены стили карточек, изображений, текста и CTA в `backend/static/css/style.css`.

### Меню и карточки блюд
- В модель `MenuItem` добавлены поля `portion_label` и `portion_tone_rgb` в `backend/models/menu_item.py`.
- Backend теперь автоматически нормализует вес/объём блюда из `item.txt`:
  - поддержаны поля вроде `weight`, `portion`, `grams`, `volume`;
  - для порции рассчитывается цветовой акцент бейджа в `backend/app.py`.
- В карточках меню на странице `backend/templates/menu.html` добавлен бейдж порции поверх фото блюда.
- Пересобран нижний блок карточки блюда и обновлена адаптивность карточек меню в `backend/static/css/style.css`.
- На главной странице выбор популярных блюд переведён на случайную выборку через `SystemRandom` в `backend/routes/main_routes.py`.

### Промо, акции и контент меню
- Загрузка промо-элементов в `backend/app.py` теперь пропускает шаблонные заглушки для `reklama` и `akciya`.
- Обновлены `item.txt` у существующих блюд и добавлены данные по весу/порции для меню.
- Позиция `ФАБ рыба` заменена на новую карточку `ФАБ-1500`.
- Добавлена новая позиция `Z-Закузка` в `backend/static/menu_items/Z-Закузка/`.
- Добавлена новая акция `Пройди опрос` в `backend/static/promo_items/akciya/Купи банку и пройди опрос/`.

### Иконки и декоративные элементы
- В проект добавлены новые SVG-ассеты: `bell4.svg`, `profile4.svg`, `hands.svg`, `callendar.svg`, `table.svg`, `person3.svg` и связанные варианты.
- Нижняя навигация переведена на новые иконки уведомлений и профиля в `backend/static/css/style.css`.
- Для страницы меню добавлены декоративные `hands`, включая отдельную мобильную версию и JS-подстройку положения под zoom/размер окна.
- Для страницы бронирования добавлен боковой декоративный календарь.
- Со страницы уведомлений удалены старые декоративные изображения `human2_frame_ball.webp` и `Vorona.webp`.

### Данные и служебные файлы
- `backend/bookings.json` очищен: тестовая бронь удалена, файл приведён к пустому массиву `[]`.
- Обновлена модульная документация проекта в `.docx`, сохранена резервная копия перед обновлением.


## 15.03.2026

### Меню: карточки блюд и граммовка
- Карточки блюд в `templates/menu.html` и `static/css/style.css` доработаны эволюционно без смены общего визуального паттерна:
  - сохранены свёрнутое и раскрытое состояния;
  - сохранён текущий тёплый тёмный премиальный стиль карточки.
- Для блюд добавлена граммовка/объём порции:
  - backend читает значение из `weight` / `portion` / `grams` / `volume` в `backend/static/menu_items/*/item.txt`;
  - граммовка больше не задаётся кодовыми дефолтами и управляется только структурой `menu_items`.
- Граммовка перенесена на фото блюда:
  - отображается как компактный premium-meta overlay;
  - на mobile и desktop используется единый паттерн размещения на медиазоне.
- Для точки рядом с граммовкой добавлена плавная цветовая логика:
  - меньшие порции ближе к кремово-кофейному тону;
  - большие порции плавно уходят к тёплому терракотово-оранжевому.

### Меню на mobile
- Мобильные стили меню очищены от сломанных вложенных `@media`-блоков в `static/css/style.css`, из-за которых часть mobile-правил парсилась нестабильно.
- Для мобильных карточек меню возвращён desktop-подход к композиции:
  - фото снова работает как левая медиазона;
  - текст и действия сохраняют layered-layout вместо отдельного вертикального mobile-паттерна.

### Главная и уведомления
- На главной странице в hero-блок добавлен декоративный слой `table.svg`, который заполняет свободную область внутри плитки без сдвига текста и кнопки.
- На `/notifications` удалены декоративные `.webp`-изображения из шаблона `templates/notifications.html`.

### Нижняя навигация и графика
- Обновлены SVG-иконки нижней навигации:
  - для уведомлений используется `bell4.svg`;
  - для профиля используется обновлённая SVG-иконка профиля.
- На главной обновлён крупный декоративный персонаж справа:
  - вместо старого `.webp` подключена SVG-версия.

### Главная: блок «Что нового»
- Карточки акций и рекламы в `templates/index.html`, `static/css/style.css` и `static/js/app.js` доработаны без смены общего визуального языка:
  - карточки больше не тянут соседние по высоте при раскрытии;
  - до раскрытия каждая карточка сохраняет свою естественную высоту.
- Раскрытие карточек переработано:
  - на desktop карточки раскрываются только по наведению;
  - на mobile раскрытие и закрытие работает по нажатию;
  - раскрытие выполняется независимо для каждой карточки и не открывает соседние.
- Плавность раскрытия улучшена:
  - анимация переведена с общей высоты карточки на внутренний блок деталей;
  - это сохраняет целую медиазону и убирает обрезание изображения при раскрытии.
- Для рекламных карточек изменено поведение ссылки:
  - убрана отдельная кнопка `Открыть`;
  - нажатие по карточке с `link` теперь сразу ведёт по внешней ссылке.
- Исправлена ложная повторная анимация карточек:
  - клик вне блока больше не запускает заново анимацию закрытия для уже свёрнутых акций и рекламы.

## 18.03.2026

### Авторизация: долгоживущий токен
- Возвращена и стабилизирована долгоживущая токен-схема авторизации:
  - после входа и регистрации frontend сохраняет подписанный токен в `localStorage`;
  - токен продолжает передаваться между страницами через `auth_token` в query/form;
  - same-origin `fetch` автоматически получает `Authorization: Bearer ...`.
- В `backend/app.py` усилено восстановление сессии из токена:
  - валидный токен теперь может перезаписать устаревшую или неверную cookie-сессию;
  - это устраняет сценарий, когда токен указывает на одного пользователя, а сервер продолжает жить со старым `session["user_id"]`.
- Добавлен глобальный after-request механизм сохранения токена:
  - внутренние server-side redirect-ответы автоматически получают `auth_token`;
  - это закрывает потерю авторизации на переходах `POST -> redirect -> GET`.

### Frontend: адаптация всего сайта под токен
- В `backend/static/js/modules/authToken.js` усилена токен-инфраструктура:
  - внутренние ссылки автоматически декорируются токеном;
  - для всех форм перед отправкой автоматически добавляется скрытое поле `auth_token`;
  - для динамически добавленных ссылок и форм добавлен `MutationObserver`.
- Добавлена тихая синхронизация первой загрузки:
  - если токен уже лежит в `localStorage`, а серверная сессия ещё не поднята, frontend вызывает `POST /api/auth/session`;
  - после успешной синхронизации страница один раз перезагружается уже в полноценном авторизованном состоянии.
- В `base.html` пробрасывается текущий `current_user_id`, чтобы не делать лишнюю синхронизацию на уже валидной серверной сессии.

### Оплата: обычный checkout
- Исправлен сбой подтверждения оплаты, когда `POST /payment/confirm` возвращал ошибку из-за пропавшего `checkout_preview` в session.
- Добавлен подписанный preview-токен:
  - при переходе на оплату создаётся signed payload с preview заказа;
  - форма подтверждения несёт `preview_token`;
  - backend умеет восстановить preview либо из session, либо из signed token.
- Для AJAX-подтверждения оплаты возвращаются более предсказуемые JSON-ошибки вместо немого провала через redirect/HTML.

### Оплата: доставка
- Аналогичный signed-preview fallback добавлен и в flow оплаты доставки:
  - `delivery_payment` создаёт `preview_token`;
  - `delivery_payment_page` и `delivery_confirm` умеют восстановить preview из токена при пропаже server-side session.
- Это закрывает типовую ошибку на шаге `Оплата доставки / Проверьте данные и подтвердите оплату`, когда preview доставки терялся между экраном проверки и подтверждением.

### Профиль и состояние данных на первой загрузке
- Исправлен сценарий, когда при первом заходе страница открывалась без заказов, статусов и профиля, а после любого клика данные внезапно появлялись.
- Теперь профильные данные, брони и активные заказы подхватываются уже на первой загрузке, если токен авторизации сохранён в браузере.

### UI
- На странице оплаты доставки центрирован заголовок `Оплата доставки / Проверьте данные и подтвердите оплату`.

### Оптимизация кода без смены поведения
- На backend добавлен request-scoped cache для текущего пользователя и данных уведомлений:
  - это уменьшает повторные `load_users`, `load_bookings` и связанные вычисления в рамках одного HTTP-запроса.
- Frontend-монолит `backend/static/js/app.js` разрезан на page-specific модули:
  - `menuCatalog.js` для каталога меню и сортировок;
  - `cartDrawer.js` для корзины и mobile drawer;
  - `checkoutPaymentFlow.js` для checkout и оплаты;
  - `formEnhancements.js` для масок и валидации полей.
- `app.js` оставлен как лёгкий orchestration-layer:
  - он собирает DOM-узлы страницы и лениво подключает только нужные модули;
  - это уменьшает размер основного JS-файла и снижает риск побочных эффектов при дальнейших изменениях.

### Главная: первая загрузка и status bar
- Для главной страницы добавлен `GET /api/index-summary`:
  - endpoint отдаёт баллы и активные статусы заказов уже в текущем токен-режиме;
  - это позволяет подтягивать пользовательское состояние после первой загрузки без дополнительного клика.
- Добавлен frontend-модуль `indexSummaryHydration.js`:
  - после старта главной он один раз догружает баллы и статус заказа;
  - это снижает зависимость UI от первого server-render, если на старте сервер ещё не увидел полную сессию.
- Логика `orderStatusBar` скорректирована:
  - если активных заказов нет, секция статусов скрывается целиком и не оставляет пустое место;
  - polling `/api/order-statuses` продолжает работать только когда status bar действительно активен.

### Документация
- В `README.md` зафиксировано текущее состояние токен-авторизации, first-load гидрации главной и оптимизаций frontend/backend.
- В конец `README.md` добавлен отдельный план постепенного перехода обратно на cookie-session через режимы `token / hybrid / session`.

## 18.03.2026 (2)

### Авторизация: cookie-first с режимами миграции
- В `backend/app.py` внедрена режимная auth-политика через `AUTH_MODE`:
  - `token` сохраняет legacy token-only поведение;
  - `hybrid` делает cookie/session основным механизмом и оставляет токен только как fallback;
  - `session` полностью отключает legacy token-sync.
- Добавлен отдельный signed `auth_session` cookie:
  - cookie выставляется сервером после логина/регистрации и на последующих авторизованных ответах;
  - на первом запросе backend может восстановить Flask session из `auth_session` без query-параметров и без промежуточной страницы;
  - если cookie невалиден или пользователь по нему не найден, cookie очищается сервером.
- Это переводит прод на cookie-first схему без немедленного удаления рабочего token fallback.

### Backend: безопасная совместимость с текущим продом
- Восстановление авторизации на `before_request` переработано:
  - сначала используется `auth_session` cookie;
  - только затем, если режим разрешает, проверяется legacy `auth_token`.
- В `AUTH_MODE=session` endpoint `POST /api/auth/session` отключается и больше не используется как часть нормального auth-flow.
- Глобальный after-request sync теперь управляет `auth_session` cookie:
  - при наличии авторизованной server-side session cookie продлевается;
  - при logout или инвалидном состоянии cookie очищается.
- Redirect propagation `auth_token` ограничен только режимом `AUTH_MODE=token`, чтобы в `hybrid/session` больше не размазывать токен по URL.

### Frontend: отказ от token-as-primary
- `backend/static/js/modules/authToken.js` переписан под режимы авторизации:
  - в `token` сохранены декорирование ссылок, форм и `Authorization` для fetch;
  - в `hybrid` токен больше не протаскивается по навигации, но может тихо поднять session через same-origin `POST /api/auth/session`, если это нужно старому клиенту;
  - в `session` token bridge не используется как auth-механизм.
- В `backend/templates/base.html` убран ранний redirect с добавлением `auth_token` для `hybrid/session`:
  - он остаётся только в чистом legacy `token` режиме.
- Шаблоны `login-success.html` и `auth-bridge.html` переведены на mode-aware поведение:
  - токен сохраняется и пробрасывается в URL только там, где это действительно разрешено конфигом.

### First-load и страницы пользователя
- Cookie-first восстановление сделано так, чтобы первый заход на главную, профиль, checkout, бронь и доставку не зависел от query/form токена.
- `GET /api/index-summary` и `GET /api/order-statuses` сохраняют роль страховочной hydration-догрузки:
  - это уменьшает риск пустого first-render даже в переходный период `hybrid`.
- В `hybrid` старые клиенты с `localStorage`-токеном продолжают работать, но новые переходы уже идут по нормальной cookie/session схеме.

### Hugging Face Spaces / proxy / cookies
- В лог старта теперь явно выводятся:
  - `auth_mode`;
  - текущий `auth_session_cookie`;
  - активные cookie-флаги для hosted-окружения.
- Зафиксирован рабочий продовый профиль для HF Spaces:
  - `SESSION_COOKIE_SECURE=true`;
  - `SESSION_COOKIE_SAMESITE=None`;
  - `SESSION_COOKIE_PARTITIONED=true`;
  - `TRUST_PROXY_HEADERS=true`;
  - `PUBLIC_BASE_URL=https://mayaran-mdk2.hf.space`.

### Проверка
- Локально прогнаны smoke-сценарии через Flask test client:
  - `AUTH_MODE=hybrid`;
  - `AUTH_MODE=session`;
  - обратная совместимость `AUTH_MODE=token`.
- Отдельно проверены:
  - login / register;
  - восстановление `/api/index-summary` только по `auth_session` cookie;
  - бронирование;
  - обычная оплата;
  - доставка;
  - отключение legacy token sync в `AUTH_MODE=session`.

### Документация
- `README.md` обновлён под новую cookie-first архитектуру и больше не описывает токен как единственный основной механизм.
- В документации зафиксированы:
  - режимы `AUTH_MODE=token|hybrid|session`;
  - новый `AUTH_SESSION_COOKIE_NAME`;
  - `AUTH_SESSION_COOKIE_MAX_AGE_SECONDS`;
  - рекомендованный rollout: `hybrid` -> наблюдение -> `session`.

### Меню: автоисправление `id` в item.txt
- Runtime-fallback для невалидных и дублирующихся `id` в `backend/static/menu_items/*/item.txt` переведён в реальное автоисправление файлов.
- Если у блюда `id` пустой, строковый, неположительный или уже занят другим блюдом:
  - backend назначает следующий свободный числовой `id`;
  - затем атомарно перезаписывает `item.txt` в UTF-8 с новым `id`.
- После первого исправляющего прохода повторные загрузки меню больше не должны шуметь одними и теми же предупреждениями по тем же блюдам.

## 18.03.2026 (3)

### Авторизация: окончательный отказ от token
- Из backend удалён legacy token auth-flow:
  - убраны `AUTH_MODE`, `AUTH_TOKEN_*`, signed `auth_token` и связанная проверка на `before_request`;
  - удалён endpoint `POST /api/auth/session`;
  - удалён server-side redirect propagation через `auth_token`.
- Авторизация теперь полностью cookie-only:
  - основой остаётся Flask session;
  - signed `auth_session` cookie используется только для восстановления server-side session на первом запросе.

### Frontend: удаление token bridge
- `backend/static/js/modules/authToken.js` упрощён до обычной навигационной обёртки без `localStorage`, Bearer headers и query/form токенов.
- Из `backend/templates/base.html`, `login-success.html` и `logout.html` удалены mode-aware флаги, `auth_token`-логика и очистка токена в браузере.
- Удалён неиспользуемый шаблон `backend/templates/auth-bridge.html`.

### Документация
- `README.md` обновлён под итоговую cookie-only архитектуру.
- Из документации убраны рекомендации по `AUTH_MODE` и token fallback.

## 18.03.2026 (4)

### Локальный запуск: универсальный сценарий
- В корень репозитория добавлен кроссплатформенный `run_local.py`:
  - сам находит `backend/`;
  - создаёт `backend/.venv`;
  - устанавливает зависимости;
  - подхватывает `backend/.env.local`;
  - генерирует `FLASK_SECRET_KEY`, если он ещё не задан;
  - запускает backend через `waitress`.
- Для локального `http://127.0.0.1` заданы корректные dev-defaults:
  - `SESSION_COOKIE_SECURE=0`;
  - `SESSION_COOKIE_SAMESITE=Lax`;
  - `SESSION_COOKIE_PARTITIONED=0`;
  - `TRUST_PROXY_HEADERS=0`.
- `backend/ops/start_app.ps1` и `backend/start_up/local/start_site.bat` переведены на единый launcher, чтобы локальный запуск больше не зависел от жёстких пользовательских путей.
- `README.md` обновлён:
  - основной локальный запуск теперь `python run_local.py`;
  - убраны привязки к конкретным путям пользователя;
  - добавлены варианты для PowerShell, `.bat` и ручного запуска.

## 18.03.2026 (5)

### Безопасность: усилено хранение паролей
- Новые пароли больше не сохраняются как простой `SHA-256`.
- В `backend/app.py` добавлено современное хеширование через `werkzeug.security.generate_password_hash` с методом по умолчанию `pbkdf2:sha256:600000`.
- Добавлен env-параметр `PASSWORD_HASH_METHOD`, если потребуется сменить алгоритм централизованно.

### Миграция пользователей без поломки логина
- Старые записи пользователей с legacy `SHA-256` продолжают пускать в аккаунт.
- При первом успешном входе такой пользователь автоматически получает новый password hash, а файл/хранилище пользователей сохраняется уже в новом формате.
- Это позволяет усилить безопасность без массового сброса паролей и без ручной миграции данных перед релизом.

### Автотесты
- Добавлен `backend/requirements-dev.txt` с `pytest`, чтобы dev-зависимости не попадали в продовый runtime-набор.
- Добавлены интеграционные тесты в `backend/tests/`:
  - регистрация и сохранение нового hash;
  - вход со старым hash и его автоапгрейд;
  - восстановление авторизации только по `auth_session` cookie;
  - бронирование, обычная оплата и доставка.

### Конфигурация окружения
- Добавлен `backend/.env.example` как единый шаблон локальных и продовых переменных.
- `README.md` обновлён:
  - добавлены инструкции по `backend/.env.example` -> `backend/.env.local`;
  - описан запуск автотестов;
  - убрана устаревшая пометка о хранении паролей в простом `SHA-256`.

## 19.03.2026

### Бронирование и время

- Дочищена логика времени для бронирований и заказов:
  - проверки активности брони, истории слотов и checkout переведены на единую схему времени;
  - устранено смешение локального времени интерфейса и UTC-представления в backend;
  - аналогичная корректировка внесена и в Postgres-ветку `backend/storage/pg_store.py`.
- Исправлен сценарий, когда свободное время ошибочно считалось прошлым и возвращало `Time cannot be in the past`.
- Исправлен сценарий, когда сразу после брони checkout писал, что бронь уже не активна.
- Для схемы выбора времени доработан правый таймлайн:
  - последний слот `22:00` теперь доезжает корректно;
  - ручной ввод времени синхронизирован с диапазоном `09:00–22:00`.

### Доставка, суммы и бонусы

- Для доставки добавлен сервисный сбор `42 ₽`:
  - отображается на этапе оформления доставки;
  - сохраняется в заказе;
  - учитывается в итоге и начислении бонусов.
- Вынесен единый helper расчёта сумм заказа в `backend/services/order_totals.py`:
  - `items_total`;
  - `service_fee`;
  - `points_applied`;
  - `payable_total`;
  - `bonus_earned`.
- На единый расчёт переведены обычный checkout, доставка и уведомления, чтобы суммы и бонусы не расходились между экранами.
- Исправлен баг, когда заказ, полностью оплаченный баллами, в уведомлениях и истории мог показывать полную сумму и начислять бонусы.
- Исправлен баг доставки, из-за которого в уведомлениях могло отображаться `+0` бонусов.

### Статусы заказов

- Исправлен расчёт ETA и таймеров доставки:
  - устранён сдвиг примерно на `+2 часа` из-за неверного разбора naive ISO-времени;
  - статус `Готовим заказ` больше не должен преждевременно падать в `00:00`.
- Расчёты фаз статусов и оставшегося времени переведены на более аккуратное округление, чтобы таймер не обнулялся раньше конца стадии.
- Улучшено плавное раскрытие подробностей очереди заказов в status bar.

### Уведомления и реклама

- Заглушка рекламы в уведомлениях заменена на реальные promo-элементы из `backend/static/promo_items`.
- В уведомлениях показывается одна случайная promo-карточка за раз.
- Рекламная карточка стала кликабельной целиком и ведёт на внешний сайт, если задан `link`.
- Обновлена адаптация promo-блока под телефон и desktop:
  - фото выводится широким баннером;
  - увеличен текст;
  - убраны лишние рамки;
  - добавлен фирменный бейдж `Акция` / `Реклама`.
- В уведомлениях и профиле для пустых состояний добавлены CTA на бронирование столика.

### Checkout, payment и история заказов

- На checkout добавлена более прозрачная информация:
  - длительность брони;
  - ожидаемое начисление бонусов.
- На экране оплаты добавлена детализация:
  - сумма заказа;
  - списанные баллы;
  - к оплате;
  - начислится бонусов.
- В истории заказов добавлены более явные бейджи типов заказа: `Доставка` и `В зале`.
- В деталях заказа доставки добавлены строки:
  - `Сумма заказа`;
  - `Сервисный сбор`;
  - `Итого`.
- На успешных экранах оплаты обычного заказа и доставки добавлена кнопка `Домой`.

### Профиль и формы

- Профиль переработан визуально:
  - убран отдельный блок `Баланс баллов`;
  - вместо буквенного аватара используется фирменный квадрат с иконкой ресторана;
  - имя пользователя стилизовано крупнее и дороже, с автоподгонкой размера под длинные имена.
- Улучшен блок `+ Привязать новую карту`:
  - форма раскрывается и закрывается плавно каждый раз;
  - текст кнопки анимированно меняется на `Отмена`;
  - отключено случайное выделение текста кнопки.
- Добавлено подтверждение после успешной привязки карты.
- Для полей карты добавлена живая визуальная валидация:
  - зелёная подсветка для корректного значения;
  - красная подсветка для невалидного.

### Меню и общие UI-правки

- Для карточек меню исправлено всплытие по `hover`:
  - карточки и неоновое свечение больше не режутся соседними контейнерами;
  - поднят приоритет карточки над соседями.
- На desktop скорректировано поведение декоративного `hands.svg`, чтобы при расширении окна элемент плавнее смещался ближе к центру композиции.
- Для `checkout` шапка приведена к тому же порядку, что и в `menu`: стрелка назад слева, затем иконка ресторана и текст.
- Убраны лишние точки в пустых состояниях:
  - `Пока нет активных броней`
  - `Пока нет уведомлений о бронировании`

### Технические улучшения

- Добавлены регрессионные тесты для:
  - расчёта сумм и бонусов;
  - доставки с сервисным сбором;
  - таймзонной логики брони.
- Локально прогнаны проверки:
  - `python -m compileall backend`
  - `backend\\.venv\\Scripts\\python.exe -m pytest backend\\tests`


## 19.03.2026(2)

### Рефакторинг backend-структуры

- `backend/app.py` раздроблен на более узкие сервисные модули без изменения пользовательского поведения.
- Из `backend/app.py` вынесены:
  - `backend/services/auth_session.py` - signed cookie, восстановление session, CSRF и request-scoped auth helpers;
  - `backend/services/menu_content.py` - загрузка меню и promo, чтение `item.txt`, Redis-кэш меню;
  - `backend/services/storage_facade.py` - фасад JSON/Postgres storage, prune заказов и file-locking;
  - `backend/services/passwords.py` - password hashing, проверка и мягкий апгрейд legacy hash.
- `backend/app.py` сокращён до роли точки сборки Flask-приложения, конфигурации и регистрации маршрутов.

### Совместимость и проверка

- Сохранены совместимые экспортируемые helper-функции из `app.py`, на которые опираются тесты и внутренние сценарии:
  - `hash_password`
  - `verify_password`
  - `verify_and_upgrade_password`
  - `issue_auth_session_cookie`
  - `verify_auth_session_cookie`
  - `issue_checkout_preview_token`
  - `verify_checkout_preview_token`
- После рефакторинга выполнена перепроверка:
  - импорт приложения;
  - smoke-check через Flask `test_client`;
  - `pytest` - результат `6 passed`.

## 20.03.2026

### Desktop-first admin panel

- Добавлена отдельная admin-зона под `/admin` без ломки клиентского интерфейса:
  - `backend/routes/admin_routes.py`
  - `backend/services/admin_service.py`
  - `backend/templates/admin/*`
  - `backend/static/css/admin.css`
  - `backend/static/js/admin.js`
- Реализованы разделы:
  - dashboard;
  - заказы;
  - брони;
  - доставка;
  - меню;
  - акции и реклама;
  - аналитика;
  - пользователи;
  - контент;
  - журнал действий.
- Admin UI собран как desktop-first зона:
  - отдельный layout;
  - sidebar;
  - sticky top bar;
  - mobile lock screen вместо кривой мобильной адаптации.

### Admin access и audit log

- Проверка доступа в админку переведена на существующую Postgres-схему:
  - `admin_users` - источник прав;
  - `admin_actions` - журнал действий.
- В `backend/storage/pg_store.py` добавлен bootstrap для `admin_users` и `admin_actions` без редизайна storage layer.
- Для опасных действий добавлено логирование в `admin_actions`:
  - отмена и смена статуса заказов;
  - отмена броней;
  - статусы и отмена доставки;
  - правки бонусов пользователя;
  - изменения menu/promo.
- `backend/app.py` получил безопасный fallback:
  - если admin-модуль недоступен при старте, публичное приложение всё равно поднимается.

### Orders, bookings и delivery

- В админке реализованы списки и карточки заказов, броней и доставки поверх текущих таблиц:
  - `users`
  - `bookings`
  - `orders`
  - `order_items`
  - `user_cards`
- Все даты и время в admin UI переведены в российский формат `ДД.ММ.ГГГГ` / `ДД.ММ.ГГГГ ЧЧ:ММ`.
- Доработаны desktop-компоновки фильтров:
  - раздельные поля в заказах;
  - компактная строка фильтров в бронях;
  - отдельные поля имени/телефона/адреса в доставке.
- В заказах и доставке русифицированы статусы:
  - `Готовится`
  - `Готов`
  - `В пути`
  - `Выдан`
  - `Отменён`
- Для доставки:
  - получатель и телефон разведены в две строки;
  - расширен блок действий;
  - статусы доставки в UI маппятся на существующий `orders.status` без новой persistence-модели.

### Dashboard и analytics

- Реализован admin dashboard с KPI-карточками:
  - заказы в работе;
  - активные брони;
  - delivery в работе;
  - выручка за сегодня;
  - отмены за сегодня;
  - просроченные доставки.
- Добавлены блоки:
  - требует внимания;
  - ближайшие брони;
  - сегодняшние заказы;
  - последняя активность.
- В аналитике:
  - графики выручки и заказов по дням;
  - donut `Зал / Доставка`;
  - топ блюд и отмены;
  - подписи дат приведены к формату `день.месяц`;
  - в легенде donut добавлены цветовые маркеры рядом с `Зал` и `Доставка`.

### Admin menu

- Раздел `Меню` переведён из CRUD-списка в form-first сценарий:
  - убран верхний toolbar со списком/поиском;
  - форма получила автозаполнение следующего `id`;
  - поле категории переведено в select по существующим категориям;
  - `Featured` и `Активно` стилизованы под тот же checkbox-стиль, что и `Использовать баллы` в checkout.
- Добавлен live-конструктор карточки блюда:
  - превью обновляется при вводе полей;
  - фото подтягивается сразу после загрузки файла;
  - расширен правый preview-panel под desktop.
- Добавлена отдельная кнопка `Открыть меню`, ведущая в клиентское `/menu`.
- Поиск по блюдам перенесён в клиентское меню сайта:
  - `backend/templates/menu.html`
  - `backend/static/js/modules/menuCatalog.js`
  - `backend/static/js/app.js`
  - `backend/static/css/style.css`

### Promo / ads

- Раздел `Акции и реклама` переведён на типозависимую форму:
  - для `akciya` скрываются рекламные поля и ссылка;
  - для `reklama` скрываются lore-поля акции.
- `start_at` и `end_at` русифицированы:
  - `Начало показа`
  - `Конец показа`
- `Активно` переведено на тот же checkbox-стиль, что и в admin menu.
- Добавлен конструктор promo-карточки с live-preview и фото.
- Список промо переработан:
  - увеличен отступ между типом и текстом;
  - улучшен перенос длинных строк;
  - расширен правый блок удаления, чтобы `Причина` и `Удалить` не ломались.

### Profile и admin entry

- В профиль добавлена кнопка перехода в `/admin` только для пользователей из `admin_users`.
- Кнопка стилизована как служебное secondary-действие рядом с `Выйти`.

### Проверка

- После серии изменений локально перепроверялись:
  - `python -m py_compile ...`
  - `backend\\.venv\\Scripts\\python -m pytest tests -q`
- На текущем этапе regression-check даёт `9 passed`.

## 20.03.2026 (2)

### Admin panel: статусы, аналитика и UX

- Исправлено расхождение между статусами в admin panel и фактическим runtime-таймлайном заказов:
  - завершённые заказы и доставки больше не зависают в `Готовится`;
  - завершённая доставка отображается как `Доставлен`, а не `Выдан`;
  - просроченная доставка больше не считается для уже завершённых заказов.
- Русифицированы ключевые элементы admin panel:
  - `Dashboard` -> `Панель`;
  - `Admin zone` -> `Панель администратора`;
  - `Quick view` -> `Посмотреть`;
  - приведены к русскому виду названия разделов, служебные лейблы и статусы.
- Доработан раздел пользователей:
  - убрана кривая приписка `admin`;
  - имя администратора оформлено неоновой растягивающейся плашкой;
  - улучшен layout блока изменения баланса и добавлена история правок бонусов.
- Для admin UI добавлены единые кастомные выпадающие списки и выровнены кнопки действий.
- В аналитике:
  - добавлены оси, горизонтальная прокрутка графиков и фиксированная ось `Y`;
  - добавлен график использования `Зал / Доставка`;
  - таблица блюд переведена с урезанного top-list на полный список за выбранный период;
  - исправлено переполнение фильтров и дат на длинных периодах.

### Оптимизация backend и admin panel

- В списке броней убран `N+1`: количество связанных заказов считается в основном SQL-запросе.
- В аналитике расчёт метрик и дневных рядов переведён на SQL-агрегации вместо полного прохода по заказам в Python.
- В Postgres bootstrap добавлены индексы под admin-фильтры и связи заказов/броней.
- В `backend/services/menu_content.py` добавлен локальный TTL-кэш для:
  - публичного меню;
  - admin menu content;
  - promo content.
- В `backend/services/auth_session.py` добавлен request-scoped cache списков пользователей и бронирований.
- В admin routes сокращены повторные загрузки menu/promo-данных в рамках одного запроса.
- Для `audit log` добавлен кэш справочников фильтрации с автоматическим сбросом после новой admin action.
- Для длинных списков в admin panel добавлена серверная пагинация:
  - `orders`;
  - `users`;
  - `audit log`.
- Поверх пагинации добавлены UX-улучшения:
  - выбор количества строк на странице;
  - быстрый переход на нужную страницу;
  - сохранение текущих фильтров и поиска при навигации между страницами.

### Стабильность и схема данных

- Исправлено падение `/admin/analytics` на старой Postgres-схеме:
  - analytics больше не падает, если в `orders` ещё нет колонки `service_fee`;
  - в bootstrap схемы добавлен `ALTER TABLE orders ADD COLUMN IF NOT EXISTS service_fee INTEGER NOT NULL DEFAULT 0`.

### Проверка

- Добавлены регрессионные тесты для:
  - рендера статусов в фильтре заказов;
  - SQL-агрегации аналитики;
  - локального кэша menu/promo;
  - request-cache в auth/session;
  - устранения повторной загрузки admin menu content;
  - кэша audit filter options;
  - рендера пагинации в admin users;
  - сохранения `per_page` в admin users.
- Текущий regression-check: `19 passed`.

### Что можно сделать дальше по оптимизации

- Перевести `orders` в admin panel на более жёсткую серверную пагинацию ещё до Python-постобработки effective status.
- Добавить SQL-агрегации и/или materialized cache для dashboard KPI, чтобы не считать их из широких выборок на каждый заход.
- Вынести `order status timeline` в более компактный server-side helper или persisted snapshot, если объём заказов вырастет.
- Добавить пагинацию и лимиты для detail-блоков истории в карточках пользователя, заказа и брони.
- Добавить lazy-loading или уменьшенные превью для тяжёлых изображений меню и promo в admin-разделах.
- При необходимости ввести короткий cache TTL для dashboard и analytics с ручным сбросом после admin action.



## 23.03.2026

### Новые блюда

- Добавлены
  - Вода из-под крана
  - Карпаччо из картошки
  - Компот из сухофруктов позапрошлого гола
  - Консоме из кубика «Галина Бланка» с пенкой из Маргарина
  - Крем-брюле из сгущёнки
  - Мороженое «Пломбир из морозилки»
  - Ризотто алла паuper
  - Солянка сборная по-бедному
  - Тартар из доширака
  - Утиная ножка конфи по-студенчески
 - Филе миньон из сосисок «Малютка»
 - Фуа-гра из вчерашнего хлеба
 - Чай из пакетика
 - Шоколадный фондан «Лава нищеты»


## 24.03.2026

### Promo, checkout и начисление баллов

- Добавлено отдельное DSL-ядро(Combo Builder Operations Script) акций в `backend/services/promotions/`:
  - AST, parser, validator, evaluator, applier, engine и checkout-helper для promo;
  - поддержка условий и наград вида `ID(...)`, `ID.<type>`, `ORDER.SUM`, `POINTS(...)`, `DISCOUNT_RUB(...)`, `DISCOUNT_PERCENT(...)`, `GIFT(...)`;
  - отдельная валидация reward mode, лимитов, окна активности и ссылок на существующие блюда/типы меню.
- Модель `PromoItem` расширена DSL-полями:
  - `condition`, `reward`, `notify`, `reward_mode`,
  - `limit_per_order`, `limit_per_user_per_day`,
  - `start_at`, `end_at`,
  - `dsl_valid`, `dsl_error`.
- Подтверждение оплаты больше не доверяет старому `preview`:
  - для `/payment/confirm` и delivery confirm позиции заказа заново собираются из актуального меню;
  - promo, скидки, `promo_points` и итоговые суммы теперь пересчитываются на сервере повторно перед записью заказа.
- `checkout preview token` теперь привязан к `user_id`, чтобы нельзя было подтвердить чужой или устаревший preview.
- В расчёте totals добавлена поддержка `discount_total`:
  - скидка вычитается до списания баллов;
  - `payable_total` и `bonus_earned` считаются уже от суммы после скидки и применения баллов.
- Для promo-акций выровнены правила времени и приоритета:
  - окна активности и дневные лимиты считаются в `APP_TIMEZONE`;
  - сортировка promo в UI и в runtime приведена к одному порядку по приоритету.
- Для checkout добавлен серверный promo preview:
  - отдельный route для пересчёта promo и скидок по текущей корзине;
  - frontend checkout/payment теперь использует пересчитанные promo-поля и уведомления.
- В `checkout` добавлен отдельный UI-блок применённой акции:
  - при соблюдении условий акции показывается название promo и её эффект;
  - блок оформлен в едином зелёном статусном стиле;
  - promo пересчитывается по текущей корзине до перехода на страницу оплаты.
- Страница `payment` приведена к тому же promo-представлению:
  - применённые акции, бонусные баллы и скидки показываются отдельным зелёным summary-блоком.
- В `Уведомлениях` DSL-акции снова отображаются как обычные promo-карточки без зелёного статуса применения:
  - зелёная подсветка оставлена только там, где акция реально применяется к заказу.
- Доработано отображение promo-summary в checkout и payment:
  - блок `Акция применена` больше не показывается, если нет реально применённых акций;
  - `notify` больше не выводится в checkout и payment;
  - `notify` остаётся только для пост-оплатных уведомлений.
- Для информационных акций `class=akciya` добавлен отдельный режим без исполняемого скрипта:
  - если в `item.txt` заполнены только `id`, `class`, `name`, `lore`, такая акция считается валидной для показа;
  - такие акции остаются в блоке акций и не попадают в runtime-расчёт promo.
- Шаблон `backend/static/promo_items/akciya/item.txt` упрощён до минимального формата информационной акции.
- Акция `Купи банку и пройди опрос` возвращена в `class=akciya` без фиктивного reward/condition:
  - отображается как акция;
  - не требует служебной заглушки в script-части;
  - не участвует в оплате как исполняемая promo.

### Promo admin и диагностика

- В admin service исправлена работа с promo без фотографии:
  - поиск папки акции теперь идёт по `class + id`, а не только по `photo`;
  - текстовые акции без изображения можно корректно обновлять и удалять.
- При загрузке promo контента добавлена серверная валидация DSL:
  - невалидные `akciya` скрываются из публичной выдачи;
  - причина ошибки сохраняется в `dsl_error` и отдаётся в admin/public metadata.
- В backend добавлена временная диагностика promo:
  - логирование загрузки promo-элементов;
  - логирование невалидного DSL у акций;
  - логирование применённых promo на этапах `/payment` и `/payment/confirm`.
- В admin panel доработан helper-конструктор DSL внутри существующего блока `Конструктор промо`:
  - helper работает только для `akciya`, для `reklama` он отключается;
  - добавлены builder-контролы для `condition` и `reward`;
  - текстовые поля DSL сохранены как основной источник истины и по-прежнему редактируются вручную.
- Для admin helper добавлена серверная кнопка `Проверить DSL` с явной цветовой индикацией результата:
  - успешная проверка отображается зелёным;
  - ошибка валидации отображается красным.
- UI helper в admin panel русифицирован для удобства:
  - переведены подписи полей, конструкторов условия и награды, а также служебные статусы проверки DSL.
- Подготовлена отдельная документация по синтаксису акций:
  - `backend/CBO_SCRIPT_V1.md`
  - пользовательское название DSL переименовано в `Combo Builder Operations Script` (`CBO Script`).

### Меню и верхняя панель категорий

- Верхняя панель категорий в `/menu` переработана под ограниченный основной набор:
  - как pill-кнопки оставлены `Все`, `Горячие блюда`, `Закуски`, `Салаты`;
  - все остальные категории автоматически попадают в dropdown `Ещё`.
- `Ещё` переведён на тот же visual/UI pattern, что и dropdown сортировки:
  - единая кнопка, menu-pattern, логика открытия/закрытия и активного состояния.
- Если выбрана категория из `Ещё`, сама кнопка показывает активный внутренний пункт:
  - плавно расширяется под текст выбранной категории;
  - мягко возвращается в нейтральное состояние при выборе быстрой категории.
- Исправлено наложение dropdown `Ещё` над карточками меню:
  - список открывается верхним плавающим слоем и больше не уходит под сетку блюд.
- Слегка уменьшено поле `Поиск по меню`, чтобы освободить место в верхней панели.
- Для активной категории добавлен лёгкий вертикальный акцент без ломки layout.
- При открытии корзины на desktop убрано принудительное размытие меню:
  - сохранён сдвиг интерфейса, но убран `blur` у основного контента.

### Проверка

- Добавлены регрессионные тесты для:
  - парсинга и валидации promo DSL;
  - применения `once/per_match`, лимитов, gift и discount priority;
  - привязки `checkout preview token` к пользователю;
  - повторного пересчёта promo при `payment confirm`;
  - удаления promo без фотографии из admin panel.
- Текущий targeted-check: `27 passed`.

### Postgres для акций и меню

- Акции `akciya` переведены на Postgres:
  - добавлены таблицы `promotions` и `promotion_applications`;
  - загрузка `akciya` идёт из БД, `reklama` остаётся файловой;
  - сохранение и удаление `akciya` в admin panel больше не пишет `item.txt`, а работает через Postgres.
- Дневные лимиты применения акций переведены на отдельную историю `promotion_applications`, а не на анализ прошлых заказов.
- Для `akciya` добавлен режим информационной акции без DSL:
  - можно сохранить только `name` и `lore`;
  - пустые `condition` / `reward` не валятся на валидации;
  - в admin panel в `reward_mode` добавлен вариант `нет`.
- Каталог блюд переведён на Postgres:
  - добавлена таблица `menu_items`;
  - публичное меню и admin menu читаются из БД;
  - сохранение блюда через `/admin/menu` пишет в Postgres;
  - изображения по-прежнему остаются в `backend/static/menu_items/...`, а в БД хранится путь.
- Для старых файлового меню и акций добавлена bootstrap-миграция в БД при первом запуске Postgres-режима.

### Автосогласование с хоста

- Добавлен startup-sync контента с хоста в БД:
  - при старте backend перечитывает файлы меню и акций;
  - выполняет `upsert` в `menu_items` и `promotions`;
  - записи, которых больше нет среди файлов хоста, автоматически удаляются из БД.
- В admin dashboard добавлена кнопка `Автосогласование`, запускающая тот же sync без перезапуска сайта.
- В audit log добавлено отдельное действие `content_autosync` с summary синхронизации.

### Promo, доставка и уведомления

- Для доставки отключено применение `akciya`:
  - исполняемые акции работают только для заказа к столику;
  - `reklama` и информационные promo-блоки продолжают отображаться как контент.
- В уведомлениях и на главной одновременно показывается только один promo-элемент:
  - либо одна реклама;
  - либо одна акция.
- Исправлена работа промо-фото из БД:
  - `photo_path` используется как основной источник пути к изображению;
  - поддержаны реальные имена файлов, а не только шаблон `photo.webp`.

### Мобильное меню и корзина

- На мобильных `/menu` и `/delivery` верхний `Поиск по меню` убран из панели и перенесён в корзину.
- На его месте добавлен dropdown `Тип`:
  - для mobile он показывает полный список категорий, включая `Все` и основные типы;
  - для desktop сохранён отдельный overflow-контрол `Ещё` только для дополнительных категорий.
- Для мобильного dropdown подтянут тот же визуальный паттерн и анимации, что и у desktop-контролов.
- Исправлено перекрытие blur при открытии корзины:
  - `cart-overlay`, hint и drawer подняты выше панели фильтров и нижней навигации;
  - overlay теперь корректно перекрывает весь экран.

### Проверка после доработок

- Локальный regression-check после перевода menu/promo в Postgres и добавления autosync:
  - `backend/tests/test_admin_panel.py`
  - `backend/tests/test_auth_and_flows.py`
  - `backend/tests/test_promotion_dsl.py`
- Актуальный targeted-check: `47 passed`.

## 25.03.2026

### Admin panel: брони и фильтры

- В карточке брони в admin panel добавлен дополнительный отступ перед кнопкой `Отменить бронь`, чтобы кнопка не стояла вплотную к блоку деталей.
- После отмены брони из её detail-страницы admin panel теперь делает возврат в общий список броней вместо перезагрузки уже удалённой карточки с сообщением `Бронь не найдена`.
- В фильтрах разделов `Заказы` и `Доставка` выровнены по одной линии:
  - селект `На странице`;
  - кнопка `Применить`;
  - быстрые кнопки `Сегодня`, `Последний час` и остальные preset-фильтры.

### Автосогласование контента

- Логика `Автосогласования` изменена:
  - отсутствующие на диске блюда больше не удаляются из Postgres, а переводятся в `active = false`;
  - отсутствующие акции и реклама также больше не удаляются, а мягко отключаются через `active = false`.
- Обновлены тексты dashboard и toast-уведомлений:
  - вместо формулировки `удалено` теперь используется `отключено`.
- Startup/log output для autosync приведён к той же терминологии, чтобы состояние синхронизации одинаково читалось в UI и в логах.

### Проверка

- Выполнен targeted-check:
  - `backend/tests/test_admin_panel.py`
- Результат: `27 passed`.

## 25.03.2026

### Backend refactor: этап 1, query-based access

- Убраны основные full-scan чтения на Postgres path для пользовательских маршрутов:
  - `/orders`
  - `/orders/<id>`
  - `/profile`
  - `/notifications`
  - главная страница
- В storage facade и Postgres storage добавлены targeted user queries:
  - получение пользователя по `id`
  - выборка заказов пользователя
  - выборка одного заказа пользователя
  - выборка броней пользователя
- Бизнес-логика уведомлений и активных заказов переведена на работу от уже выбранных пользовательских данных, а не от полной загрузки всех заказов и броней.

### Admin: пагинация и bounded queries

- `/admin/orders` переведён на SQL-пагинацию с `COUNT + LIMIT/OFFSET`.
- `/admin/delivery` переведён на SQL-пагинацию с `COUNT + LIMIT/OFFSET`.
- Для админской доставки добавлены:
  - навигация по страницам
  - выбор `per_page`
- Dashboard и admin list paths переведены на более ограниченные запросы вместо лишней Python-side обработки широких коллекций.

### Persisted effective status для заказов

- Runtime-логика вычисления статуса заказа вынесена в отдельный модуль:
  - `backend/services/order_status.py`
- Для заказов добавлены persisted-поля:
  - `effective_status`
  - `effective_status_updated_at`
  - `is_delivery_overdue`
- Admin read paths и пользовательские Postgres read paths теперь читают persisted status вместо вычисления effective status на каждом выводе списка.
- Добавлен bounded refresh persisted-полей заказов:
  - по конкретному заказу
  - по заказам конкретного пользователя
  - по активным заказам
- Исправлено поведение `effective_status_updated_at`:
  - timestamp больше не перезаписывается без реальной смены effective status.

### JSON/Postgres согласование по статусам

- JSON storage тоже нормализует persisted-поля заказов, чтобы поведение не расходилось между JSON и Postgres режимами.
- Сохранение заказов в JSON приведено к единой нормализации статуса и overdue-флага.

### Изоляция JSON/Postgres в request paths

- Убраны прямые `load_users/load_orders/load_bookings`, `save_*` и `json/storage locks` из route-обработчиков:
  - `auth`
  - `booking`
  - `checkout/payment`
  - `delivery`
  - `profile card actions`
- В `StorageFacade` добавлены явные операции для пользовательских flows:
  - поиск пользователя по телефону
  - создание пользователя
  - обновление password hash
  - добавление и удаление карт
  - проверка/создание брони
  - отмена брони с отменой связанных dine-in заказов
  - создание заказа
  - атомарная корректировка баланса пользователя
- Для Postgres добавлены targeted mutation/query helpers вместо JSON-style full collection rewrites в основных пользовательских операциях.
- `AuthSessionService` переведён на targeted callbacks для:
  - загрузки пользователя по `id`
  - загрузки броней текущего пользователя
- Full-scan fallback в `AuthSessionService` сохранён как обратная совместимость, но основной app wiring теперь использует точечные запросы.

### SQL и bootstrap

- Обновлён bootstrap-скрипт `backend/sql/neon_init.sql`:
  - новые поля в `orders`
  - новые индексы под persisted status и overdue delivery
- Добавлена отдельная миграция для существующей БД:
  - `backend/sql/task5_order_effective_status.sql`
- Миграция делает:
  - добавление колонок
  - backfill persisted status
  - backfill overdue delivery flag
  - создание индексов `CONCURRENTLY`

### Нормализация time fields

- Для fresh Postgres schema переведены legacy timestamp-поля с `TEXT` на typed columns:
  - `users.created_at`
  - `user_cards.created_at`
  - `bookings.created_at`
  - `orders.created_at`
  - `orders.cancelled_at`
  - `orders.effective_status_updated_at`
  - `admin_users.created_at`
- В Postgres storage добавлены UTC-aware converters:
  - запись typed timestamp-значений в БД
  - обратная сериализация в привычные ISO-строки на выходе в приложение
- Для admin Postgres reads добавлена нормализация temporal values, чтобы шаблоны и сервисы не зависели от сырого `datetime/date/time` формата psycopg.
- Добавлена отдельная staged-миграция для существующей БД:
  - `backend/sql/task6_normalize_time_fields.sql`
- Миграция:
  - валидирует legacy text timestamps перед cast
  - трактует naive legacy timestamps как UTC
  - переводит blank optional fields в `NULL`
  - переводит обязательные `created_at` поля в `TIMESTAMPTZ`

### Разделение oversized admin service

- Из `backend/services/admin_service.py` вынесена query-логика заказов в отдельный модуль:
  - `backend/services/admin_order_queries.py`
- Из `backend/services/admin_service.py` вынесена dashboard/analytics query-логика в отдельный модуль:
  - `backend/services/admin_dashboard_queries.py`
- Из `backend/services/admin_service.py` вынесены backoffice read-модели пользователей и броней:
  - `backend/services/admin_directory_queries.py`
- Из `backend/services/admin_service.py` вынесен audit feed и фильтры журнала действий:
  - `backend/services/admin_audit_queries.py`
- Из `backend/services/admin_service.py` вынесены admin write-операции:
  - `backend/services/admin_command_ops.py`
- Из `backend/services/admin_service.py` вынесен content-management для menu/promo:
  - `backend/services/admin_content_management.py`
- `AdminService` сохранён как фасад с прежним публичным API для route-слоя, но теперь делегирует:
  - фильтры и пагинацию заказов
  - деталку заказа
  - dashboard KPI/read models
  - analytics aggregates/charts
- Дополнительно `AdminService` теперь делегирует:
  - списки и деталки пользователей/броней
  - audit log listing
  - audit filter options
  - table occupancy reads
  - admin command mutations по заказам, броням и бонусам
  - menu/promo content write flows и promo DSL preview/validation
- Это уменьшает связность между admin query-paths, dashboard calculations и content-management частью сервиса без изменения HTTP-интерфейсов.

### UTF-8 runtime cleanup for content reads

- В `backend/services/menu_content.py` runtime parsing `item.txt` переведён на UTF-8 / UTF-8-SIG без `cp1251` fallback.
- Legacy fallback-кодировка `cp1251` сохранена только в explicit import/sync logic на стороне `backend/storage/pg_store.py`.
- Это уменьшает зависимость обычных application read-paths от legacy encoding heuristics и приближает content runtime к правилу UTF-8 only.

### DB-first promo content for Postgres

- В Postgres-режиме `load_promo_items()` больше не подмешивает файловый `reklama` в обычные runtime reads.
- Promo feed в Postgres теперь читается целиком из БД, включая:
  - `akciya`
  - `reklama`
- Таблица `promotions` расширена полями:
  - `text`
  - `link`
- `backend/storage/pg_store.py` обновлён для:
  - чтения обоих promo-классов из БД
  - upsert/sync `reklama` через ту же таблицу `promotions`
  - legacy disk import both `akciya` and `reklama`
- `backend/services/admin_content_management.py` теперь сохраняет и удаляет `reklama` через Postgres path, оставляя файлы только для медиа.
- Добавлена SQL-миграция для существующей БД:
  - `backend/sql/task8_promotions_reklama_db_first.sql`
- `backend/sql/neon_init.sql` дополнен актуальными admin/menu/promo таблицами и индексами для fresh bootstrap.

### Проверка

- После изменений выполнен полный локальный regression-check:
  - `backend/tests/test_admin_panel.py`
  - `backend/tests/test_auth_and_flows.py`
  - `backend/tests/test_promotion_dsl.py`
- Актуальный regression-check: `49 passed`.

## 25.03.2026

### Admin panel: UX и autosync

- В detail-странице брони добавлен отступ перед кнопкой `Отменить бронь`, чтобы action не прилипал к блоку деталей.
- После отмены брони detail-страница admin panel теперь возвращает в список всех броней вместо перезагрузки уже удалённой записи с ошибкой `Бронь не найдена`.
- В разделах `Заказы` и `Доставка` выровнены по одной линии:
  - селект `На странице`
  - кнопка `Применить`
  - preset-фильтры `Сегодня`, `Последний час` и остальные быстрые диапазоны.
- Автосогласование контента больше не удаляет отсутствующие на диске блюда, акции и рекламу из Postgres:
  - записи переводятся в `active = false`
  - dashboard, toast и startup-логи приведены к формулировке `отключено`.

### Postgres startup и legacy schema stabilization

- В Postgres bootstrap добавлена автоматическая нормализация legacy temporal columns при старте приложения:
  - `users.created_at`
  - `user_cards.created_at`
  - `bookings.created_at`
  - `orders.created_at`
  - `orders.cancelled_at`
  - `orders.effective_status_updated_at`
  - `admin_users.created_at`
  - `admin_actions.created_at`
  - `menu_items.created_at`
  - `menu_items.updated_at`
  - `promotions.start_at`
  - `promotions.end_at`
  - `promotions.created_at`
  - `promotions.updated_at`
  - `promotion_applications.applied_at`
- Исправлен порядок Postgres schema bootstrap:
  - старый `DEFAULT` у legacy `TEXT`-колонок теперь снимается до `ALTER COLUMN ... TYPE TIMESTAMPTZ`
  - это устраняет startup crash вида `default for column "created_at" cannot be cast automatically to type timestamp with time zone`.
- Для card removal в Postgres устранён runtime crash на старой схеме:
  - после нормализации `user_cards.created_at` удаление карты снова работает как чистый SQL path без временных compatibility-обходов.

### DB-first content reads и startup validation

- В Postgres-режиме promo/runtime content больше не уходит молча в fallback на файловый диск при ошибке чтения БД.
- `MenuContentService` теперь делает явную проверку readiness Postgres content reads на старте:
  - menu
  - promotions
- Это убирает "полуживое" состояние, когда приложение визуально стартует, но часть контента обслуживается уже не тем storage path.

### SQL/docs synchronization

- Обновлён `backend/sql/task6_normalize_time_fields.sql` под актуальный список temporal columns и реальные legacy timestamp formats.
- `README.md` актуализирован под текущее состояние проекта:
  - changelog и DSL-документация теперь лежат в `backend/doc/...`
  - `reklama` и `akciya` в Postgres-режиме читаются из БД
  - autosync отключает записи вместо удаления
  - regression-check обновлён до актуального значения.

### Frontend performance и отзывчивость интерфейса

- Карточки меню и страница блюда переведены с CSS `background-image` на обычные `<img>`:
  - это открыло нативную lazy-loading загрузку через `loading="lazy"` и `decoding="async"`
  - первые карточки остаются быстрыми для первого экрана, остальные больше не тянут изображения заранее.
- Обновлены шаблоны:
  - `backend/templates/menu.html`
  - `backend/templates/delivery_menu.html`
  - `backend/templates/menu-item.html`
- Разгружен общий CSS:
  - тяжёлые стили меню вынесены в `backend/static/css/menu-page.css`
  - checkout/payment/profile/order-detail стили вынесены в `backend/static/css/payments.css`
  - `backend/static/css/style.css` сокращён и теперь не грузит часть страничных стилей глобально на всех маршрутах.
- Page-scoped CSS подключён только там, где он реально нужен:
  - меню и карточка блюда
  - checkout и payment
  - profile и order-detail.
- Для status bar заказа уменьшена фоновая нагрузка:
  - polling больше не крутится в скрытой вкладке
  - при возврате во вкладку выполняется корректное возобновление обновления
  - добавлен backoff при ошибках, чтобы не долбить сервер одинаково часто при проблемах.
- Облегчены постоянно работающие анимации status bar:
  - shimmer прогресса сделан мягче и реже
  - убран `drop-shadow` у delivery truck
  - `statusTextFade` больше не анимирует `filter`
  - в `prefers-reduced-motion` бесконечная анимация грузовика полностью отключается.
- Ускорено ощущение переключения между нижними вкладками:
  - искусственная задержка навигации в `bottomNavMotion.js` сокращена с `300ms` до `120ms`.

### Проверка

- Выполнен полный локальный regression-check:
  - `backend/tests/test_admin_panel.py`
  - `backend/tests/test_auth_and_flows.py`
  - `backend/tests/test_promotion_dsl.py`
- Актуальный regression-check: `56 passed`.

## 25.03.2026

### Фронтенд: меню, главная и бронирование

- Обновлены карточки блюд на главной и в меню:
  - фото переведены на `img`-разметку;
  - тип блюда выровнен в строке заголовка;
  - квадратная подача изображений и доработка сетки карточек.
- Пересобраны стили страницы меню:
  - обновлены фильтры и блок сортировки;
  - доработана desktop-корзина справа;
  - обновлены состояния кнопки `В корзину` / `Убрать`.
- Обновлены визуальные эффекты карточек и навигации:
  - нижняя навигация получила увеличенные SVG-иконки и обновлённый pulse;
  - hover-настроение карточек меню переведено в тёплый акцент.

### Бронирование зала (`/reserve`)

- Доработана desktop-раскладка схемы зала:
  - таймлайн даты и времени закреплён справа от схемы;
  - легенда и подписи под залом выровнены относительно схемы.
- Исправлены слои и эффекты схемы:
  - восстановлен blur при наведении и выборе свободного столика;
  - возвращён фоновый неон под столиком с корректным позиционированием;
  - сохранено центрированное модальное окно бронирования на desktop.
- Уточнена логика hover и выбора стола в `tableTooltip.js`:
  - обновлено позиционирование неона;
  - стабилизированы состояния hover/active для свободных столов.

## 31.03.2026

### Frontend performance: page-scoped JS, checkout и рендер

- Разделён глобальный клиентский JS по страницам:
  - общий `backend/static/js/app.js` больше не грузится на всех маршрутах через `base.html`;
  - добавлен лёгкий базовый entry `backend/static/js/base.js`;
  - page-specific entry points вынесены в `backend/static/js/pages/` для:
    - главной
    - меню
    - checkout / delivery checkout
    - payment / delivery payment
    - profile
    - reserve
  - общие безопасные куски вынесены в `backend/static/js/shared/`.
- Облегчён `checkout`:
  - из HTML убран полный `menu_catalog`;
  - `checkout` теперь опирается на данные позиций из корзины, без встраивания всего каталога меню в страницу.
- Убрана лишняя клиентская гидрация главной:
  - стартовый JS для `/` больше не делает автоматический повторный запрос ` /api/index-summary` сразу после SSR, когда данные уже встроены в HTML.
- Оптимизирован расчёт бейджа уведомлений:
  - `notifications_count` больше не требует лишнего получения полных тяжёлых структур там, где нужен только `count`;
  - в `app.py` добавлен совместимый fallback, чтобы приложение не падало при рассинхронизации конструктора `AuthSessionService` между средами.

### Frontend performance: CSS и menu UX

- Продолжена разгрузка глобального CSS:
  - page-specific стили дополнительно вынесены из `backend/static/css/style.css` в `backend/static/css/menu-page.css` и `backend/static/css/payments.css`.
- Доработана desktop-корзина меню:
  - восстановлена плавная анимация открытия и закрытия drawer;
  - корзина снова плавно меняет высоту при изменении состава;
  - при большом количестве позиций desktop-корзина может расти почти до высоты окна, а не упирается слишком рано.
- Доработана mobile-вёрстка карточек меню:
  - увеличен отступ между фото и текстовым блоком;
  - после неудачного расширения карточек mobile-раскладка меню откатана к более компактному виду;
  - `название` и `тип блюда` снова живут в компактной mobile-компоновке рядом с фото;
  - фото в mobile-карточках прижато к левому краю карточки и снова занимает всю её высоту при квадратной подаче.
- Кнопка `В корзину` на карточках меню приведена к стилю кнопки `В меню` из блока `Ваши брони`.

### Проверка

- Выполнена локальная проверка пользовательских маршрутов и import-check приложения.
- Локально пройден узкий regression-check по пользовательским flow:
  - `backend/tests/test_auth_and_flows.py`

### Admin panel: desktop layout и визуальная полировка

- Пересобрана desktop-раскладка админ-панели:
  - снято глобальное ограничение ширины admin-страниц;
  - основной контент расширен под wide-screen desktop;
  - навигация и контент приведены к более устойчивой двухколоночной сетке без ложного центрирования.
- Доработаны списки и таблицы админки:
  - `Заказы` растянуты на полную рабочую ширину;
  - в detail-странице заказа action-кнопки выровнены по центру и собраны аккуратнее;
  - `Доставка` приведена к читаемой desktop-таблице без смешивания адреса и статуса.
- Доработаны фильтры и toolbars:
  - в `Брони` все фильтры собраны в один ряд;
  - `Состояние` встроено в общий ряд фильтров;
  - кнопка `Применить` уменьшена и поставлена последней в строке;
  - в `Заказы` и `Доставке` выровнены toolbar-блоки и быстрые пресеты.
- Перестроены редакторы `Меню` и `Акции и реклама`:
  - для `Промо` добавлена устойчивая двухколоночная desktop-сетка;
  - кнопка `Конструктор промо` добавлена рядом с основными действиями;
  - поля `Начало показа` / `Конец показа` больше не налезают на соседнюю колонку;
  - в `Меню` ограничена ширина `Конструктора карточки`, чтобы предпросмотр не растягивался на очень широких мониторах.
- Доработана `Аналитика`:
  - страница полностью пересобрана в формат dashboard, а не набора разрозненных карточек;
  - добавлены hero-блок, единая панель фильтров, ряд KPI с динамикой к прошлому периоду и главный блок трендов;
  - справа от главного графика вынесены сравнительный блок `зал / доставка`, отмены и быстрые сигналы;
  - нижняя часть разделена на понятные секции: популярные блюда, проблемные позиции и полная таблица ассортимента;
  - расширен набор серверных агрегатов для аналитики: сравнение с предыдущим периодом, channel summary, insights, last sale и слабые позиции.
- Обновлена навигация админки:
  - ссылка `Вернуться на сайт` перенесена в нижнюю часть sidebar и визуально отделена от основного меню.
- Упрощён индикатор загрузки админ-панели:
  - убрана pixel-анимация и промежуточные декоративные состояния;
  - оставлен компактный неблокирующий текстовый статус вида `Грузим {раздел}` для переходов по админ-навигации.

### Проверка

- Выполнена локальная проверка admin panel:
  - `backend/tests/test_admin_panel.py`
- Актуальный результат: `32 passed`.

### 01.04.2026 HF Spaces: диагностика производительности, статика и оптимизация изображений

- Улучшен production-старт контейнера на HF:
  - `Dockerfile` переведён с базового sync-режима `gunicorn` на `gthread`;
  - добавлены настраиваемые `workers` и `threads` через env, чтобы уменьшить очереди при одновременной отдаче HTML и статики;
  - итоговая команда запуска осталась совместимой с Docker parser HF.
- Для Flask добавлено кэширование статических файлов:
  - настроен `SEND_FILE_MAX_AGE_DEFAULT`;
  - добавлен env-параметр `STATIC_CACHE_MAX_AGE_SECONDS` с дефолтом `3600`.
- Для локальной оптимизации медиа добавлен инструмент batch-сжатия изображений:
  - `compress_images.bat` в корне проекта;
  - `backend/ops/resize_images.py` для квадратного ресайза `1:1` с размером по умолчанию `480x480`;
  - поддержаны `.webp`, `.png`, `.jpg`, `.jpeg`;
  - обработка выполняется in-place с настраиваемым `size` и `quality`.
- Выполнена массовая оптимизация изображений меню:
  - тяжёлые карточки блюд приведены к компактному квадратному формату;
  - суммарный вес каталога изображений `menu_items` снижен примерно до `5.56 MB`.



## 02.04.2026

### Хостинг и запуск на SpaceWeb

- Поднят production-запуск проекта на SpaceWeb через `mod_wsgi`.
- Подготовлены и проверены файлы запуска для shared-hosting сценария:
  - `wsgi.py`
  - `.htaccess`
- Подключена локальная PostgreSQL-база SpaceWeb вместо внешнего Neon.
- Подтверждено, что приложение рендерит основные страницы на новом хосте:
  - `/`
  - `/menu`
  - `/delivery`

### Диагностика производительности и сети

- Подтверждено, что основная деградация при открытии сайта из РФ не объясняется временем рендера Flask-приложения.
- Для ответов добавлены серверные тайминги:
  - `Server-Timing`
  - `X-Render-Time-Ms`
- Выявлено, что приложение отвечает быстро, а задержки в проблемном сценарии возникают вне application-layer.

### Сессии и публичные GET-запросы

- Убрано лишнее создание session/cookie на публичных GET-запросах и статике.
- Публичные страницы и `static` перестали лишний раз помечаться cookie-зависимыми.
- Сохранена работа CSRF для форм и приватных сценариев.

### Канонизация slug и путей к фото

- Добавлен общий модуль канонического именования контента:
  - `backend/services/path_naming.py`
- Введено правило:
  - `slug` хранится в ASCII-формате;
  - `photo_path` хранится в каноническом виде;
  - русские названия остаются в `name`, а не в файловых путях.
- Примеры целевого формата:
  - `Пирог Мина` -> `pirog-mina`
  - `menu_items/pirog-mina/photo.webp`
  - `Пройди опрос` -> `proydi-opros`
  - `promo_items/akciya/proydi-opros/photo.webp`

### Админка и сохранение контента

- Админское сохранение блюд переведено на канонический ASCII `slug`.
- Админское сохранение акций и рекламы также переведено на канонический ASCII `slug`.
- Формирование `photo_path` для блюд, акций и рекламы унифицировано в одном формате.
- Исправлена ошибка фронта в админке:
  - `Cannot read properties of null (reading 'redirectUrl')`
  - устранён доступ к `currentAction` после `closeModal()`.

### Автосогласование host -> Postgres

- Disk sync для `menu_items` больше не пишет в БД сырые имена каталогов с диска.
- Disk sync для `promotions` больше не пишет в БД сырые кириллические `slug` и старые file-path.
- Автосогласование теперь использует тот же канонический формат, что и админка.
- Это предотвращает повторное занесение битых `slug` и `photo_path` в БД после ручной очистки файловой структуры.

### Фото блюд, акций и рекламы

- Для блюд сохранён fallback-поиск реального фото на диске, если канонический путь в БД ещё не совпадает с фактическим именем файла.
- Добавлен аналогичный fallback для промо-элементов:
  - акции;
  - реклама.
- Это позволяет пережить промежуточное состояние после пересинхронизации БД, пока файловая структура на хосте ещё не полностью приведена к каноническому виду.

### Обработка проблемных путей и кодировок

- Усилена нормализация путей к статике при чтении контента.
- Добавлены защитные сценарии для битых `photo`-путей, чтобы страницы не падали на `UnicodeEncodeError`.
- Добавлен служебный скрипт для восстановления имён файлов статики:
  - `backend/ops/repair_static_filenames.py`

### Тесты и техническая проверка

- Добавлены тесты на:
  - кириллические названия блюд при записи в Postgres;
  - канонизацию `slug` и `photo_path` при legacy disk sync меню;
  - канонизацию `slug` и `photo_path` при legacy disk sync промо.
- Изменённые Python-файлы прошли синтаксическую проверку `py_compile`.
- Отдельные адресные проверки логики канонизации и disk sync выполнены через локальные Python-сценарии.

## 02.04.2026

### Авторизация и телефон

- Логин и регистрация переведены на единый телефонный формат с маской `+7`.
- Нормализация и серверная проверка номера синхронизированы между frontend и backend.
- Исправлено поведение поля телефона при полном выделении и `Delete`/`Backspace`:
  - поле теперь корректно сбрасывается до `+7`.

### Checkout и правая колонка

- Исправлена верстка правой колонки на checkout при активной акции.
- Блок `Итого` и promo-плашка стали устойчивее к длинным строкам и хостовым отличиям рендера.
- Для `payments.css` добавлено версионирование подключения, чтобы хост не держал старый кэш стилей.

### Главная страница и популярные блюда

- Переработаны карточки в секции `Меню → Популярное`.
- Карточки переведены в формат галереи с overlay по hover, без разрушения сетки.
- Отключён отдельный preview блюда по переходу в `/menu/<id>`:
  - карточки больше не ведут на detail-page;
  - старый route detail-page редиректит обратно в `/menu`.

### Reserve и фон страницы

- Исправлен фон страницы `reserve` для сценариев с сильным zoom и маленькой высотой окна.
- Убран визуальный шов между градиентом и основным фоном внизу страницы.
- Фон страницы `reserve` сделан непрерывным и устойчивым к различным размерам viewport.

## 03.04.2026

### Главная страница и популярные блюда

- Блок `Меню -> Популярное` на главной переведён с случайного выбора на данные из админской аналитики продаж.
- Для главной добавлен безопасный fallback:
  - если аналитика недоступна, используются `featured/popularity` из меню;
  - поведение не ломается в режиме без Postgres.
- Переработана анимация карточек `Популярное`:
  - текст раскрывается отдельным выездом;
  - blur-слой теперь проявляется плавно через `opacity`, без движения самого blur.
- Исправлен визуальный шов в overlay-слое карточек `Популярное`.

### Меню и доставка

- Исправлена mobile/layout-логика карточек меню:
  - цена и кнопка `В корзину` зафиксированы у нижнего края карточки;
  - карточка стала устойчивее к разной длине описания.
- Убрано дублирование шаблонов `menu.html` и `delivery_menu.html`:
  - общая карточка вынесена в partial;
  - общий список меню вынесен в partial;
  - различия между обычным меню и доставкой оставлены только в обвязке и классах кнопок.
- Исправлена регрессия рендера `/menu` после вынесения partial:
  - восстановлен корректный контекст приоритетной загрузки первых изображений;
  - маршруты `/menu` и `/delivery` снова рендерятся без `500`.

### Авторизация и поля ввода

- Расширен набор фраз для статус-бара заказов на главной по всем стадиям:
  - ожидание;
  - готовка;
  - подача в зал;
  - доставка;
  - завершение.
- Исправлено смещение неоновой подсветки у полей телефона и пароля на страницах логина и регистрации:
  - glow больше не считается от контейнера с лейблом;
  - фокус и телефонная подсветка визуально привязаны к самому `input`.

### Reserve и схема зала

- Исправлено mobile-поведение `/reserve`:
  - нижний sheet даты/времени ограничен шириной viewport;
  - мобильная summary-панель даты/времени перестроена под узкие экраны.
- Исправлено desktop-отображение неона на странице `/reserve`:
  - боковое свечение схемы больше не режется контейнером зала;
  - сохранено ограничение лишнего горизонтального скролла на уровне страницы.

### Админка

- В модальном окне автосогласования админки кнопка закрытия закреплена в правом верхнем углу.

### SVG и статика

- Проведена lossless-оптимизация тяжёлых декоративных SVG:
  - `person3.svg`;
  - `table.svg`.
- После регрессии восстановлен корректный `orelll.svg`.
- Для оптимизированных SVG сохранена валидная XML-структура с `xmlns`, чтобы не ломать рендер в `img`.

### Техническая проверка

- Изменённые Python-файлы проверены через `py_compile`.
- Шаблоны после вынесения partial проверены на корректный Jinja-синтаксис.
- Выполнены адресные проверки маршрутов:
  - `/menu`
  - `/delivery`
  - `/login`
  - `/register`
  - `/reserve`

## 04.04.2026

## Исправление mobile-поведение `/reserve`:

  - нижняя навигация и fixed-блоки больше не растягиваются из-за горизонтального overflow; 

## улучшения /meun

- 'Сортировать'
  - Наследовало анимации как у поля 'Тип'
  - По стандарту имеет новую сортировку От А до Я(так же и английский алфавит от A до Z)
  - отображает только метод сортировки, вместо 'Сортировать (метод)'
  - теперь прибит к левой части

- Добавлена кнопка 'Цена' для более точной сортировки по цене
  - Получила анимации как у схожи элементорв 
  - собственное выпадающее меню
  - для mobile меню схожее с mobile корзиной и подсказкой.

- Тип 'салаты' убран из быстрого доступа

- Методы сортировки теперь имеют вид: |Тип|Цена|От А до Я|

## 05.04.2026

### Меню (desktop): карточки и конец скролла

- Исправлен визуальный разрез внизу страницы `/menu` на desktop при прокрутке к концу:
  - скорректировано поведение внутренних блоков карточек;
  - убрано ложное растягивание карточки по высоте;
  - устранён эффект «полосы» на фоне рядом с `menu-side-hands`.
- Исправлено переполнение контента карточки:
  - блок `.menu-card__content` больше не вылезает на соседнюю карточку;
  - hover/focus-область карточек ведёт себя стабильно.

### Меню: граммовка и цветовой индикатор

- Исправлена нормализация единиц веса/объёма в бейдже порции:
  - `кг` теперь корректно интерпретируется как килограммы, а не граммы;
  - значение `2,2 кг` больше не считается как `2,2 г`;
  - цветовая логика точки у граммовки/мл работает по нормализованному значению.

### Админка и профиль: текст «О нас» из БД

- В админ-панели (раздел `Панель`) добавлен блок редактирования текста «О нас».
- Текст сохраняется в БД и выводится в `/profile` отдельным блоком под историей заказов.
- Добавлен backend-flow для чтения/записи контента профиля:
  - хранилище значения в БД;
  - методы сервиса админки;
  - маршрут сохранения из dashboard;
  - передача данных в профильный шаблон.
- Исправлен POST формы сохранения:
  - добавлен `csrf_token`, чтобы запрос не отбрасывался middleware.

### Форматирование текста «О нас»

- Добавлена поддержка форматирования текста из админки:
  - переносы строк (новые строки в тексте отображаются в блоке профиля);
  - поддержка `**жирного**` текста.
- Форматирование выполняется безопасно:
  - пользовательский текст экранируется;
  - разрешается только целевой ограниченный формат.

### Визуальная доводка блока «О нас»

- Подправлены отступы и типографика блока «О нас» под общий ритм страницы профиля.
- Добавлена подсказка в админке по синтаксису форматирования (`Enter` и `**текст**`).
- Обновлена версия подключения `style.css` в `base.html` для принудительного сброса кэша статики.

### Promo DSL V2 (CBO Script V2)

- Реализовано расширение DSL акций до V2 без поломки V1:
  - добавлена поддержка `dsl_version=2`;
  - в `condition` добавлены `==`, `!=`, `NOT`;
  - зафиксирован приоритет операторов `NOT > AND > OR`;
  - сохранены скобки и базовые сравнения `> < >= <=`.
- Расширены и нормализованы метрики условий:
  - добавлен синтаксис `TYPE(<type>).QTY|SUM|UNIQUE_QTY`;
  - для `GROUP(...)` добавлен `UNIQUE_QTY`;
  - для агрегата `ID` добавлен `ID.UNIQUE_QTY`;
  - `ORDER.SUBTOTAL` добавлен как целевой V2-алиас суммы заказа;
  - `ORDER.SUM` сохранён как совместимый алиас.
- Расширен синтаксис reward:
  - сохранены совместимые награды V1 (`POINTS`, `DISCOUNT_PERCENT`, `DISCOUNT_RUB`, `GIFT`);
  - добавлены таргетированные скидки:
    - `DISCOUNT_PERCENT(value, TARGET=ORDER|GROUP(...))`
    - `DISCOUNT_RUB(value, TARGET=ORDER|GROUP(...))`;
  - добавлен reward:
    - `CHEAPEST_FREE_FROM_GROUP(...)`.
- Обновлён runtime применения акций:
  - таргетированные скидки считаются по выбранной базе (`ORDER` или `GROUP`);
  - для `CHEAPEST_FREE_FROM_GROUP(...)` применяется скидка по самой дешёвой позиции из группы;
  - логика приоритета скидок между акциями сохранена.
- Обновлены validator и ограничения:
  - `CHEAPEST_FREE_FROM_GROUP` запрещён с `reward_mode=per_match`;
  - в V2 запрещён legacy-синтаксис `ID.<type>...` (нужно `TYPE(...)...`);
  - сохранена строгая проверка ссылок на существующие `item_id`/типы/группы.

### Хранилище и схема БД для DSL V2

- В модель `promotions` добавлено поле `dsl_version`:
  - схема инициализации обновлена в `backend/sql/neon_init.sql`;
  - SQL-скрипт промо обновлён в `backend/sql/task4_promotions.sql`.
- В Postgres-слое добавлена авто-миграция:
  - `ALTER TABLE promotions ADD COLUMN IF NOT EXISTS dsl_version INTEGER`;
  - чтение/запись `dsl_version` включены в `load_promotions` и `upsert`.
- В модель `PromoItem` и в pipeline `menu_content/admin_content_management` добавлена передача `dsl_version`.

### Admin panel: helper для V2

- В форме `admin/promo` добавлен выбор версии DSL (`dsl_version`).
- DSL-helper обновлён под V2:
  - источник типа переведён на `TYPE(<type>)`;
  - добавлена метрика `UNIQUE_QTY`;
  - добавлены операторы `==` и `!=`;
  - добавлены настройки `TARGET` для `DISCOUNT_*`;
  - добавлен конструктор `CHEAPEST_FREE_FROM_GROUP(...)`.
- Улучшен текст результата проверки DSL:
  - в ответе явно показывается версия (`v1`/`v2`) и тип награды.

### Документация и тесты

- Добавлена отдельная документация:
  - `backend/doc/CBO_SCRIPT_V2.md`.
- README обновлён ссылкой на документацию V2.
- Расширены тесты promo DSL:
  - добавлены кейсы на `NOT`, `!=`, `TYPE(...)`, `UNIQUE_QTY`,
    `TARGET=GROUP(...)`, `CHEAPEST_FREE_FROM_GROUP(...)`,
    а также regression для обратной совместимости V1.
- Текущая адресная проверка:
  - `backend/tests/test_promotion_dsl.py` -> `20 passed`.


## 07.04.2026

- `Fixed` Исправлено поведение мобильных карточек меню: при выборе другого блюда предыдущая карточка больше не исчезает и не “всплывает” повторно после завершения анимации.

- `Changed` Улучшена логика reveal-анимации карточек в каталоге: класс `menu-card--reveal` теперь автоматически очищается после завершения анимации (с fallback по таймеру для стабильности на разных устройствах).


## 08.04.2026

### Security hardening: auth, checkout и promo links

- Убраны небезопасные fallback-секреты:
  - `FLASK_SECRET_KEY` переведён в обязательную переменную окружения;
  - запуск backend больше не должен опираться на заранее известный ключ из кода.
- Добавлена защита от перебора пароля на `/login`:
  - введено ограничение по числу неуспешных попыток;
  - после серии неудачных логинов временно включается блокировка.
- Закрыт сценарий повторного подтверждения checkout/delivery:
  - preview заказа получил одноразовый `preview_id`;
  - повторное подтверждение уже использованного preview отклоняется.
- Усилена валидация promo-ссылок:
  - публичные и admin-path ссылки нормализуются;
  - запрещены небезопасные схемы вида `javascript:`.

### Учебный режим оплаты

- Платёжный flow упрощён под учебный проект:
  - из профиля убран ввод реального номера карты, срока действия и держателя;
  - вместо этого доступны безопасные демо-методы оплаты.
- Обновлены пользовательские экраны:
  - `/profile` теперь подключает `demo MIR` и `demo VISA`;
  - `/checkout` и экран оплаты показывают учебный способ оплаты вместо модели реальной карты.
- В backend и storage-path больше не передаются полные карточные реквизиты:
  - сохраняются только безопасные display-данные демо-метода;
  - тесты обновлены под новую модель оплаты.

### Данные, соглашение и внутренний доступ

- Из репозитория убраны чувствительные локальные данные и служебные секреты из рабочих файлов проекта.
- Актуализировано пользовательское соглашение:
  - текст переведён с устаревшей гостиничной модели на текущий сайт ресторана;
  - зафиксирован учебный демо-режим оплаты;
  - убраны старые формулировки про отдельные уровни привилегий.
- Внутренний доступ описан и выровнен как единый служебный backoffice-уровень:
  - `администратор / менеджер`;
  - без отдельной иерархии ролей внутри проекта.

### Тесты

- Добавлены и обновлены адресные regression-тесты для:
  - rate limit логина;
  - одноразового подтверждения заказа и доставки;
  - безопасной обработки promo links;
  - учебного сценария подключения способа оплаты.
- Текущая адресная проверка:
  - `backend/tests/test_auth_and_flows.py` -> `22 passed`.

### Регистрация и пользовательское соглашение

- В `/register` добавлено обязательное согласие с пользовательским соглашением:
  - чекбокс стилизован под тот же паттерн, что и `#usePoints` в checkout;
  - без подтверждения соглашения регистрация не проходит server-side проверку.
- Добавлена отдельная публичная страница `/user-agreement`:
  - соглашение открывается в новой вкладке прямо из формы регистрации;
  - текст рендерится как обычная HTML-страница сайта.
- Для стабильного открытия на хосте источник соглашения переведён на UTF-8 текстовый файл:
  - `backend/doc/user_agreement.txt`;
  - runtime больше не зависит только от чтения `.docx` по кириллическому пути.

### Профиль: возврат card flow и фикс масок

- Профильный сценарий привязки карты возвращён к предыдущему UX:
  - снова используются поля `Номер карты`, `Срок`, `Имя`;
  - убраны тексты про демо-оплату из формы профиля.
- Восстановлены прежние JS-маски и форматирование для `/profile`:
  - номер карты;
  - срок действия;
  - имя держателя.
- Для `profile.js` и связанных модулей добавлен cache-bust:
  - это принудительно сбрасывает старый браузерный кэш и подхватывает актуальную версию масок.

### Актуальная проверка

- Последняя адресная проверка после правок по регистрации, соглашению и профилю:
  - `backend/tests/test_auth_and_flows.py` -> `24 passed`.

## 15.04.2026

### Незакоммиченные изменения

- Усилена защита SQL-слоя в `backend/storage/pg_store.py`:
  - добавлены allowlist-проверки для внутренних имён таблиц и колонок;
  - убрана прямая подстановка идентификаторов в SQL для внутренних helper-функций;
  - обновлены `_count_rows`, `_normalize_timestamptz_column` и `_next_integer_id`.

- В рабочем дереве также зафиксированы незакоммиченные изменения файлов:
  - изменён `backend/storage/pg_store.py`;
  - удалён `backend/doc/Модульная_документация_СВОИ_РЕСТОРАН_v8.docx`;
  - добавлен `backend/doc/Модульная_документация_СВОИ_РЕСТОРАН_v9_updated.docx`;
  - добавлены `backend/orders.json` и `backend/users.json`;
  - присутствует новый файл `h origin --force --all`.

## 22.04.2026

- обновленна документация тестов